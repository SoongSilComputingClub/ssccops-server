# AGENTS.md

SSCC(숭실컴퓨팅클럽) 지원서 관리 백엔드 — Spring Boot 3.5 / Java 17 / Gradle.

## 빌드 · 테스트 · 린트

- 전체 빌드: `./gradlew build` (compileJava → checkstyle → spotless → test → jacoco 순서로 전부 수행)
- 테스트만: `./gradlew test`
- 단일 테스트 클래스: `./gradlew test --tests "org.sscc.ssccopsserver.domain.member.service.MemberServiceImplTest"`
- 단일 테스트 메서드: `./gradlew test --tests "*.MemberServiceImplTest.메서드명"`
- 포맷 검사: `./gradlew spotlessCheck` / 자동 정렬·수정: `./gradlew spotlessApply`
- 스타일 검사: `./gradlew checkstyleMain checkstyleTest` (설정: `config/checkstyle/checkstyle.xml`, Naver 컨벤션 변형 — 상단 주석에 원본과의 차이가 정리되어 있음)
- 로컬 실행: `local` 프로필(`-Dspring.profiles.active=local`)이 PostgreSQL 접속 정보(`DB_HOST`/`DB_PORT`/`DB_NAME`/`DB_USERNAME`/`DB_PASSWORD`)와 Supabase JWKS URI(`SUPABASE_URL`), CORS 허용 오리진(`FRONTEND_URL`), Cloudflare R2 접속 정보(`R2_ACCOUNT_ID`/`R2_ACCESS_KEY_ID`/`R2_SECRET_ACCESS_KEY`/`R2_BUCKET_NAME`, ssccops#113 · 공개 읽기 도메인 `R2_PUBLIC_BASE_URL`, #161)를 요구한다. R2 엔드포인트는 `R2_ACCOUNT_ID`로 서버가 직접 조립하므로 별도 env가 없다. **`.env` 한 벌을 `docker compose`와 `./gradlew bootRun`이 함께 읽는다**(`cp .env.example .env` 후 값 채우기 — compose는 이 디렉터리의 `.env`를 자동으로, `bootRun`은 `build.gradle`이 파싱해 JVM 환경변수로 주입). 두 가지 방식이 있다:
  - `docker compose up`으로 `backend`(local 프로필)와 `postgres`를 함께 띄운다. 컨테이너 안에서는 DB 주소가 달라야 하므로 `docker-compose.yml`이 `DB_HOST=postgres`·`DB_PORT=5432`로 덮어쓴다.
  - `docker compose up postgres`로 DB만 띄우고(호스트 포트 `DB_PORT`, 기본 `15432`) 앱은 `./gradlew bootRun`으로 실행한다. `.env`의 `DB_HOST=localhost`·`DB_PORT=15432`가 그대로 쓰인다. 로컬에 PostgreSQL을 직접 설치해 쓴다면 `createdb ssccops_server_db` 후 `DB_PORT=5432`로 바꾼다.

  **주의**: `.env`와 `docker-compose.yml`의 변수 이름에 하이픈(`-`)을 쓰지 않는다. Compose의 변수 치환은 셸 파라미터 확장 문법을 따르므로 `${db-username}`은 이름이 아니라 "`db`가 없으면 문자열 `username`"으로 읽힌다 — 실제로 이 때문에 `.env` 값이 통째로 무시된 채 `POSTGRES_USER=username`·`POSTGRES_DB=name:-ssccops_server_db`로 DB가 만들어지고 앱이 인증 실패로 죽는 일이 있었다(#59). Spring 프로퍼티 키(`db-username`)를 환경변수 이름으로 그대로 쓸 수 없다.

  **로컬은 기동마다 데이터가 사라진다**(`ddl-auto: create-drop`). 그래서 회원도 매번 새로 가입해야 하고, 첫 가입자가 최고관리자가 된다(#71). 기획안 시스템 폼(`PROPOSAL`)은 명의로 쓸 회원이 있어야 세워지므로 **가입하기 전에는 없는 것이 정상**이며, 가입하는 순간 세워진다(#184). 기동 로그에 `회원이 없어 기획안 시스템 폼(PROPOSAL) 시드를 건너뛴다`가 보이면 고장이 아니라 아직 아무도 가입하지 않은 상태다. 가입한 뒤에도 폼이 없다면 그때가 진짜 문제이니 `ProposalFormSeeder`의 `log.error`를 찾을 것.
- 프로필: `local`(PostgreSQL, OTel 비활성, ddl-auto create-drop) / `dev`(ddl-auto update — 이유는 아래) / `prod`(env 변수 주입, ddl-auto update — 정식 버전 전까지 한시적, 이유는 아래, Swagger 비활성) / `test`(H2 인메모리, ddl-auto create — 테스트 실행 시 자동 적용). **`create-drop`이 아닌 이유는 `application-test.yaml`의 주석에 있다** — 모든 테스트 컨텍스트가 `testdb` 하나를 공유하는데 `create-drop`은 컨텍스트가 닫힐 때 스키마를 지워, 테스트 컨텍스트 캐시가 evict 하는 순간 남은 테스트가 "Table MBR not found"로 떨어진다.

**주의**: JPA 프로필 설정에 `database-platform`(Hibernate `dialect`)을 명시하지 않는다. Hibernate가 커넥션에서 자동 감지하며, 명시하면 `HHH90000025` 경고가 뜨고 DB 엔진을 바꿀 때 드라이버와 방언이 어긋나 깨진다.

**주의**: checkstyle의 `ImportOrder`는 `java, javax, jakarta, org, net, com, *, lombok` 그룹 순서를 엄격히 검사한다. import를 추가/이동한 뒤 checkstyle이 실패하면 순서를 수동으로 고치지 말고 `./gradlew spotlessApply`로 자동 정렬할 것 (Spotless의 `importOrder` 설정이 checkstyle 규칙과 동일하게 맞춰져 있음).

**주의**: `prod`는 `ddl-auto: none`이 **아니라 `update`다** — 정식 버전 전까지의 한시적 결정이다. `none`이던 동안에는 엔티티가 늘 때마다 배포 전에 `CREATE TABLE`·`CREATE INDEX`를 사람이 먼저 실행해야 했고, 빠뜨리면 배포가 아니라 첫 요청에서 터졌다. 스키마가 아직 이슈마다 바뀌는 단계라 그 수동 단계 자체가 실질적인 배포 실패 원인이었다.

그래서 **추가는 자동이 됐지만 나머지는 그대로 수동이다** — `update`는 새 테이블·컬럼·인덱스만 반영하고 **컬럼 삭제·이름 변경·타입 변경·널 허용 변경은 반영하지 않는다**. 그런 변경은 여전히 배포 전에 직접 실행해야 한다: `MemberEntity.authUserId`(컬럼 `auth_user_id`, 구 `spb_user_id`)처럼 컬럼명을 바꿨다면 `ALTER TABLE mbr RENAME COLUMN spb_user_id TO auth_user_id;`, `mbr.stdnt_no`를 nullable로 바꿨다면(#21, 졸업 회원 가입) `ALTER TABLE mbr ALTER COLUMN stdnt_no DROP NOT NULL;`. **`prod`에는 dev의 탈출구가 없다** — dev는 드리프트가 쌓이면 DB를 통째로 재생성하면 되지만 prod에는 데이터가 있다. 마이그레이션 도구(Flyway/Liquibase)는 정식 버전 시점에 넣고 그때 `none`으로 되돌린다.

**주의**: `dev`는 `ddl-auto: create-drop`이 **아니라 `update`다**(ssccops#83). Render 무료 티어(512MB, 공유 CPU)에서 재시작(배포·유휴 슬립 해제 포함)마다 스키마 전체를 지우고 다시 만드는 비용이, 회원·역할·CSV 이관·회의 등 테이블이 늘어나며 헬스체크 타임아웃을 넘길 만큼 무거워졌다 — 실제로 부팅 중 `HikariPool housekeeper Thread starvation`이 찍히고 배포가 `update_failed`로 반복 실패했으며, 한 번은 부팅이 "성공"했지만 `mbr_grd` 시드가 일부만 들어간 채로 떠 회원가입이 500을 냈다. `update`는 새 테이블·컬럼은 자동 반영하지만 **컬럼 삭제·이름 변경·타입 변경은 반영하지 않는다** — 지금은 `prod`도 `update`라 두 환경의 제약이 같다. 리네임·삭제가 필요한 변경을 만들면 dev DB에도 수동 `ALTER`가 필요할 수 있다(지금은 개발 단계라 드리프트가 쌓이면 통째로 재생성해도 되지만, 늘어날수록 이 트레이드오프가 부담이 된다 — Flyway/Liquibase 도입을 그때 검토한다). **`ddl-auto: update`로도 근본 원인은 해결되지 않았다** — 실제로는 부팅 중 Hibernate가 EntityManagerFactory(엔티티 26종 메타모델)를 만드는 도중 컨테이너가 OOM으로 죽고 있었다(`exit 137`, ssccops-server#107). `Dockerfile`의 `ENTRYPOINT`에 `-XX:MaxRAMPercentage=60.0 -XX:+UseSerialGC -Xss512k`를 명시해 힙 밖 메모리(메타스페이스·스레드 스택·GC 북키핑)에 여유를 남기는 것으로 해결했다. Render 무료 티어는 512MB라 이 여유가 빠듯하다 — 엔티티·리포지토리가 계속 늘면 이 캡도 다시 부족해질 수 있으니, dev가 또 `update_failed`/`exit 137`을 반복하면 이 절부터 의심할 것.

## 아키텍처

패키지 루트는 `org.sscc.ssccopsserver` (Gradle group도 `org.sscc`) — SSCC 동아리 프로젝트이므로 Spring Initializr 기본값(`com.example`)을 쓰지 않는다.

- `domain/{member,auth,operation,...}` — 도메인별로 `controller/service/repository/entity/dto/code` 하위 구조를 반복하는 계층형 패키지. 새 도메인을 추가할 때 이 구조를 그대로 따른다. (`admin`/`applyform`/`user`는 Supabase 인증 전환에 맞춰 재설계 예정이라 제거됨 — 잔재를 찾는 코드가 있다면 지우면 된다.)
- `domain/example` — 위 6계층 구조를 보여주는 참고용 템플릿 도메인(실제 기능 아님). 새 도메인을 추가할 때 이 구조를 복사해서 시작하면 된다.
- `global/config/R2Config` — Cloudflare R2(S3 호환 오브젝트 스토리지, ssccops#113) 연결용 `S3Client` 빈. R2 전용 SDK가 없어 AWS SDK v2의 S3 모듈을 그대로 쓰되, 리전은 R2가 요구하는 고정값 `"auto"`이고 `forcePathStyle(true)`가 필요하다(꺼져 있으면 R2가 모르는 가상 호스트 이름으로 요청이 나가 연결 자체가 실패한다). 같은 설정으로 `S3Presigner` 빈도 함께 만든다(#161 — 프리사이너에는 `forcePathStyle` 단축 설정이 없어 `S3Configuration.pathStyleAccessEnabled(true)`로 켠다). 두 빈의 엔드포인트·리전이 갈리면 서명은 성공하는데 R2가 거절하는 URL이 나가고, 그 실패는 서버 로그가 아니라 브라우저에서만 보인다.
  - **첫 사용처는 행사 본문 이미지 업로드(`POST /v1/events/{eventId}/images`, #161 · wave2 D6)이며 서버는 파일 바이트를 다루지 않는다** — presigned PUT URL(유효기간 10분)을 발급하고 운영 웹이 R2로 직접 PUT 한다. 멀티파트 업로드 엔드포인트를 만들지 말 것: 512MB 컨테이너는 부팅 중 엔티티 메타모델을 만들다 죽은 전례가 있고(#107) 그 위에 업로드 버퍼링을 얹으면 이미지 몇 장으로 프로세스가 죽는다. 키는 `events/{eventId}/{uuid}.{ext}`이고 확장자는 원본 파일명이 아니라 허용 목록(`EventImageType` — png·jpeg·webp·gif, SVG는 스크립트를 담을 수 있어 뺐다)의 값으로 붙인다. contentType과 확장자를 **둘 다** 보고 서로 맞아야 통과한다(400 `UNSUPPORTED_IMAGE_TYPE`). 크기 상한 10MB(413 `IMAGE_TOO_LARGE`)는 **요청이 신고한 크기**에 대한 판정이라 방어선이 아니라 안내이며 실제 강제는 버킷 정책의 몫이다. 공개 읽기 URL은 `r2.public-base-url`로 조립하고 **기본값을 두지 않아** 값이 없으면 부팅이 실패한다 — 잘못된 URL은 행사 본문 마크다운에 문자열로 굳어 되돌릴 수 없다. **DB에 아무것도 남기지 않으므로 서버는 실제로 올라왔는지 모른다**(PUT이 서버를 거치지 않는다) — 고아 오브젝트 정리는 범위 밖이다.
- `global/apipayload` — 모든 API 응답의 공통 껍데기.
  - `ApiResponse<T>` — 생성자는 private, `success`/`successWithNoData`/`created`/`fail` 정적 팩토리로만 생성.
  - `code/success/SuccessCode`, `code/error/ErrorCode` — 인터페이스이며 `CommonErrorCode`가 전역 에러를 구현하고, 도메인 전용 에러가 필요하면 같은 방식으로 도메인 패키지 아래 `code/error` enum을 구현한다. 새 에러를 추가할 때 전역(`CommonErrorCode`)에 넣을지 도메인 전용 enum에 넣을지 먼저 판단할 것.
  - `exception/GeneralException` — 서비스 레이어에서 던지는 표준 예외, `ErrorCode`를 감싼다.
  - `handler/GlobalExceptionHandler` — `@RestControllerAdvice`. `GeneralException`은 감싼 `ErrorCode` 그대로, `MethodArgumentNotValidException`/`HttpMessageNotReadableException` 등 스프링 기본 예외는 `CommonErrorCode`로 변환해 항상 `ApiResponse.fail(...)` 포맷으로 응답한다. 컨트롤러 레벨에서 별도 try/catch를 추가하지 않는다.
- `global/security` — 인증/인가. 구글 OAuth2 로그인과 자체 JWT 발급 스택은 제거됐고, 지금은 Supabase Auth가 발급한 JWT를 `spring-boot-starter-oauth2-resource-server`로 검증한다. JWKS URI(`{SUPABASE_URL}/auth/v1/.well-known/jwks.json`)는 프로필별로 다른 Supabase 프로젝트를 가리키도록 `application-{profile}.yaml`에 분리돼 있다.
  - `global/security/jwt/SupabaseJwtAuthenticationConverter` — JWT의 `sub`(UUID)로 `MemberService.findByAuthUserId(...)`를 호출해 회원을 **조회만** 한다. **인증 시점에 회원을 만들지 않는다** — 만들어 버리면 "로그인은 했지만 아직 가입하지 않은 사용자"라는 상태가 존재할 수 없고, 조회 요청 하나에도 쓰기 트랜잭션이 열린다. `mbr` 행이 생기는 유일한 경로는 회원가입 API다. `MemberEntity.authUserId`(컬럼 `auth_user_id`)는 특정 인증 벤더 이름을 쓰지 않는다 — 지금은 Supabase Auth의 사용자 ID를 담지만 인증 수단이 바뀌어도 이 컬럼명은 유지된다. JWT의 `role` 클레임은 RLS용 Postgres 역할이라 인가 판단에 쓰지 않는다.
  - principal은 `MemberEntity`가 아니라 `global/security/AuthenticatedUser`다 — `authUserId`·`email`·`name`(`user_metadata.full_name`/`name`)·`provider`(`app_metadata.provider`)와 **nullable한 `MemberEntity`**를 담는다. 가입 여부는 별도 등급 코드가 아니라 `member == null`로 표현된다.
  - 회원이 필요한 엔드포인트는 principal을 캐스팅하지 말고 **`@CurrentMember MemberEntity`**로 받는다(`global/security/resolver`). 미가입 주체는 이 리졸버 한 곳에서 **403 `SIGNUP_REQUIRED`**(`MemberErrorCode`)로 끊기므로 주입된 값은 항상 non-null이다. 401(토큰 없음·무효)과 구분되며, 프론트는 이 코드를 받으면 재로그인이 아니라 가입 화면으로 보낸다. 리졸버 등록은 `global/config/WebConfig`.
  - principal에 실린 `MemberEntity`는 **준영속**이다(인증 필터는 트랜잭션 밖). 등급·상태 같은 지연 로딩 필드를 꺼내려면 식별자만 쓰고 회원 도메인 Service로 다시 조회해야 한다 — `MemberService.getProfile(memberId)`가 조회 트랜잭션 안에서 DTO로 굳혀 돌려준다.
  - **인가는 `@RequireAuthority` + AOP다** (`global/security/authorization`, #9). `GrantedAuthority`는 여전히 비워 둔다 — 역할·권한은 화면에서 바뀌는 사용자 관리 데이터라 인증 시점에 굳히면 이미 발급된 토큰의 판정이 따라오지 않고, 권한이 필요 없는 요청에도 조회가 붙는다. 애노테이션이 붙은 핸들러에서만 DB를 보는 **지연 판정**이며, 붙지 않은 엔드포인트는 종전처럼 인증만 요구한다. 클래스에 붙이면 그 컨트롤러 전체에 걸리고 메서드 애노테이션이 이긴다.
    - 판정 규칙의 **유일한 구현은 `domain/member/service/AuthorityPolicy`**다: 회원 → 유효한 역할(`role_bgng_ymd <= 오늘 <= role_end_ymd`, 종료일 NULL이면 무기한, 오늘은 주입된 `Clock`) → 그 역할들에 부여된 권한(`role_authrt_rel`) → **자손까지 펼침**. 펼침은 위→아래 한 방향이라 하위를 가졌다고 상위가 생기지 않으며, `mbr_role_rel.rprs_role_yn`(대표 역할)은 표시용이라 보지 않는다. `role.indct_seqno`를 서열로 쓰는 판정은 **쓰지 않는다** — 분류별 표시 순번이라 '프로젝트장(PROJECT 1)'이 '국장(POSITION 4)'보다 높게 계산된다.
    - **`SUPER`는 트리의 최상위이고 그게 전부다** (#71). `EXECUTIVE`가 그 자식이라 기존 펼침만으로 모든 권한을 포함하며, `AuthorityPolicy`에 `SUPER`를 특별 취급하는 분기는 **없다** — 넣지 말 것. 웹이 저장 전 미리 보기를 위해 트리 간선으로 같은 펼침을 한 번 더 하므로(`entities/authority/model/tree.ts.previewGrants`), 서버만 특별 취급하면 체크박스에는 한 줄만 켜지는데 저장하면 전부 열리는 상태가 된다. 새 권한 코드를 추가할 때 트리에 매달지 않으면 `SUPER`가 그것을 포함하지 못한다.
    - 권한(`authrt`)은 **단일 부모 트리**이며 자식을 가진 노드가 곧 묶음 권한이다. 상위 지정 시 조상 검사로 순환을 막고(`AuthorityEntity.changeParent`), 그래도 고리가 남은 데이터를 만나면 펼침이 방문 집합으로 멈춘다. `sys_yn = true`는 코드(`AuthorityCode`)가 직접 가리키는 권한이라는 표시다 — 삭제·코드 변경을 막아야 화면 조작 한 번으로 인가가 무력화되지 않는다.
    - 거절 응답은 `@CurrentMember`와 같은 계단이다: 미인증 401 · 미가입 403 `SIGNUP_REQUIRED` · 권한 부족 403 `FORBIDDEN`(`MemberErrorCode.AUTHORITY_REQUIRED`). **404로 감추지 않는다.** 다만 애스펙트는 인자 해석 뒤에 돌므로 본문이 `@Valid`를 어기면 권한 없는 요청도 400을 먼저 받는다.
    - 화면에는 역할이나 트리가 아니라 **펼친 결과**를 내린다 — `MemberProfileResponse.capabilities`(`GET /v1/auth/session`, ssccops-web#29). 애스펙트와 같은 정책을 쓰므로 버튼과 실제 판정이 갈리지 않는다.
    - 승인자 판정(`ApprovalAuthorityPolicy`)은 이것과 층이 다르다 — 그쪽도 판정 재료는 권한(#123, 유형이 지정한 결재 권한·`APPROVAL_VOTE`)이지만 유형마다 요구 코드가 달라 `@RequireAuthority` 하나로 고정되지 않아, 서비스 레이어에서 `AuthorityPolicy`의 펼침으로 건별 판정한다.
  - 인증 실패(서명/만료 오류 등)는 `CustomAuthenticationEntryPoint`, 권한 부족은 `CustomAccessDeniedHandler`가 `ApiResponse` 포맷으로 응답한다.
  - `SecurityConfig`가 필터체인을 구성: CSRF/formLogin/httpBasic/logout 비활성화, `SessionCreationPolicy.STATELESS`, 비prod Swagger 경로와 `/actuator/health`·`/actuator/info`, 그리고 **`/public/v1/**`**만 permitAll(배포 헬스 프로브는 토큰을 붙일 수 없다 — `prometheus`·`metrics`·`loggers`는 계속 보호), 나머지는 인증 필요. **`/public/v1/**`는 업무 API 중 익명 접근이 허용되는 유일한 층이다**(#156 · 공개 행사 조회, ssccops#143 D1) — 예외를 엔드포인트 목록이 아니라 경로 접두사로 표현한 것은, `/v1/**` 아래에 익명 경로를 섞으면 여기가 경로별 permitAll 표가 되고 한 줄이 빠지거나 패턴이 넓게 잡히는 것으로 인증이 필요한 자원이 열리기 때문이다. 그러므로 **이 접두사 아래에 핸들러를 더하는 것은 permitAll을 더하는 것과 같다**. 폼의 '공개' 경로(`/v1/forms/{id}/public`)와 갈리는 지점이며 그쪽은 여전히 인증이 필요하다(응답자는 전원 회원이다). **경로별 권한 규칙을 여기 두지 않는다** — `RoleHierarchy`(ADMIN ⊃ USER ⊃ PREUSER)와 `/admin/**`은 #9에서 제거했다(실제 어휘와 대응되지 않는 잔재였고, 남기면 권한 어휘가 두 벌이 된다).
  - CORS는 `SecurityConfig.corsConfigurationSource()` 한 곳에서만 정의한다. 허용 오리진은 `frontend.url`이며 **쉼표로 여러 개**를 넣을 수 있다(Cloudflare Workers 프리뷰 도메인 대응). 시큐리티 필터체인이 먼저 처리하므로 `WebMvcConfigurer.addCorsMappings()`로 중복 정의하지 말 것 — `WebConfig`는 인자 리졸버 등록에만 쓴다.
- `domain/member` — `mbr` 행이 생기는 **유일한 경로**는 `POST /v1/members/signup`이다. 인증 시점에는 회원을 만들지 않으므로(#20) 다른 진입점을 만들지 말 것. 이 엔드포인트만 `@CurrentMember`가 아니라 `@AuthenticationPrincipal AuthenticatedUser`로 주체를 받는다 — `@CurrentMember`는 미가입 주체를 403으로 끊어 가입 자체를 막는다. 응답은 세션 조회와 같은 `MemberProfileResponse`이며, 별도 가입 응답 DTO를 두지 않는 것은 한쪽만 필드가 늘어 두 응답이 어긋나는 것을 막기 위해서다. 등급은 항상 `TEMP`로 고정하고, 등급·상태의 최초 부여는 `mbr_grd_hstry`·`mbr_stts_hstry`에 `bfr_*_cd = NULL`로 한 건씩 남긴다. `mbr.stdnt_no`는 졸업 회원을 위해 **nullable**이며 `uk_mbr_student_number`는 유지되므로 학번 미입력은 빈 문자열이 아니라 **NULL**로 저장해야 한다. 선조회만으로는 동시 요청을 못 막으므로 UNIQUE 위반(`DataIntegrityViolationException`)도 같은 409로 옮긴다.
  - **등급·상태를 바꾸는 길은 `POST /v1/members/{mbrId}/grade-changes`·`status-changes` 둘뿐이다** (#78). 회원 정보 수정에 등급·상태 필드를 두지 않는 것은, 같은 API에 섞으면 이력 없이 등급이 바뀌는 경로가 반드시 생기기 때문이다 — "등급을 바꾼다"와 "이력을 남긴다"는 나눌 수 없는 한 건이다. `mbr` 갱신과 `mbr_grd_hstry`·`mbr_stts_hstry` INSERT가 한 트랜잭션이며 이력 저장이 실패하면 갱신도 되돌아간다(`MemberChangeRollbackTest`). **변경자(`chnrg_mbr_id`)는 요청 본문이 아니라 `@CurrentMember`에서 온다** — 받아 주면 "누가 바꿨는가"를 스스로 적어 넣을 수 있어 이력이 증거가 되지 못한다. 지금과 같은 값으로의 변경은 400 `NO_CHANGE`다(통과시키면 "임시회원 → 임시회원" 행이 쌓여 실제 승급 시점을 못 찾는다). 적용 일자를 생략하면 주입된 `Clock`의 오늘이고 미래 일자는 400이다 — `mbr`은 이 요청으로 지금 바뀌므로 미래 일자를 받으면 이력과 회원이 어긋난다(예약 변경은 적용 시점에 값을 바꿔 줄 장치가 있어야 성립한다).
    - 구현이 `MemberServiceImpl`이 아니라 **`MemberChangeService(Impl)`라는 별도 빈**인 것은 순환 주입 때문이다. 탈퇴·제명 경고의 '담당 하위 업무 건수'에 `SubWorkService`가 필요한데 `SubWorkServiceImpl`은 이미 `MemberService`를 주입받으므로(AR-07), 한 빈에 두면 생성자 주입이 고리가 되어 애플리케이션이 뜨지 않는다. 회원 도메인이 운영 Repository를 직접 부르지 않는 것은 그 반대 방향 규칙과 같다 — 진입점은 `SubWorkService.countOngoingByOwner` 하나다.
    - **탈퇴(`WITHDRAWN`)·제명(`EXPELLED`) 전이는 아무것도 정리하지 않는다.** 역할을 함께 종료할지, 담당 중인 하위 업무를 어떻게 넘길지는 운영 규칙이 필요한 판단이라 별도 이슈다. 대신 남아 있는 현재 역할·미완료 담당 하위 업무 건수를 응답의 `warnings`로 실어 화면이 사람에게 알린다(`MemberChangeWarningResponse`). 남은 것이 없으면 건수 0짜리 줄을 내리지 않는다.
    - **`stts_end_prnmnt_ymd`(상태 종료 예정일)는 휴학·군휴학에만 실을 수 있고 그 밖의 상태에 실려 오면 400으로 거절한다**(`MemberStatusCode.allowsExpectedEndDate`). 조용히 버리지 않는 것은 이력 행이 `updatable = false`로 잠겨 있어서다 — 버리면 운영자는 "2027-03-02 복학 예정"을 적어 넣었다고 믿는데 어디에도 남지 않고 나중에 채워 넣을 경로도 없다. 폼 도메인이 유형과 무관한 잔여 속성을 거절하지 않고 정리하는 것과 갈리는데, 그쪽은 화면이 남긴 찌꺼기이고 이쪽은 사람이 직접 친 날짜다.
  - 권한 테이블 두 개도 이 도메인이다(ssccops#69 데이터사전). `authrt`(권한 트리, PK가 IDENTITY가 아니라 코드 문자열인 것은 `@RequireAuthority`가 그 값을 직접 가리키기 때문이다)와 `role_authrt_rel`(역할↔권한, `(role_id, authrt_cd)` UNIQUE). `data.sql`이 권한 트리(#101·#123의 결재·투표 권한 포함)와 회장·부회장·총무 → `EXECUTIVE` · 국장 → `OPERATOR` · 최고관리자 → `SUPER`(#71) · 국원 → 조회·안건·투표(#101·#123) · 역할별 결재 권한(#123) 매핑을 멱등하게 시드한다 — 프로젝트장·스터디장은 권한이 없으며 '권한 없는 역할은 아무것도 못 한다'가 기본값이다. 시드는 `role_id`를 값으로 박지 않고 역할명으로 조회해 넣는다(IDENTITY라 환경마다 다르다).
  - **최초 가입자 부트스트랩** (#71 · ssccops#71). 권한 관리 화면이 `ROLE_MANAGE`를 요구하므로 새 환경에서는 아무도 그 권한을 가질 수 없다 — 역할을 부여하려면 이미 권한이 있어야 하는 닫힌 고리다. 그래서 **`mbr` 테이블이 비어 있을 때 가입하는 첫 회원에게 `최고관리자` 역할(→ `SUPER`)을 자동 배정한다**. 판정 기준은 '회원이 한 명도 없는가' 하나이며 'SUPER 보유자가 없는가'가 **아니다**(BR-M37) — 그 기준이면 최고관리자가 탈퇴할 때마다 창구가 다시 열려 가입 순서만으로 시스템을 가져갈 수 있다. 부여는 회원이 아니라 역할에 하며(BR-M38), 그 역할은 특별히 잠기지 않는다 — 화면에서 다른 회원에게 넘겨주는 것이 정상 경로다. 구현은 `MemberServiceImpl.claimBootstrapRole`·`grantBootstrapRole` 두 메서드뿐이다.
    - 동시 가입 두 건이 모두 "비어 있다"를 보면 최고관리자가 둘이 되므로(VR-M14), 1차는 잠금 없이 세고 창구가 열려 보일 때만 부트스트랩 역할 행을 `PESSIMISTIC_WRITE`로 잠근 뒤 **다시 센다**. 순서를 뒤집으면 잠금을 기다리는 사이 앞선 트랜잭션이 커밋해 낡은 숫자로 통과한다. 1차 검사가 있어 평상시 가입에는 잠금이 걸리지 않는다.
    - 시드가 깨져 `SUPER`가 열리지 않으면 **가입을 실패시킨다**(VR-M15, 500). 통과시키면 권한 없는 첫 회원이 남아 `mbr`이 더는 비어 있지 않게 되고 창구가 영영 닫힌다. 확인은 `AuthorityPolicy.capabilityListOf`로 되물어 한다 — 매핑 행 존재만 따로 보면 "응답에는 권한이 보이는데 요청은 403"이 될 자리가 생긴다.
    - 스키마 변경이 아니라 `data.sql`의 행 네 개다(`authrt`의 `SUPER` · `role_clsf`의 `SYSTEM` · `role`의 `최고관리자` · 매핑). 이미 시드된 DB는 `EXECUTIVE`가 최상위로 들어가 있어 `up_authrt_cd IS NULL` 가드 UPDATE로 `SUPER` 아래로 옮긴다. **이미 회원이 있는 환경(dev·prod)에서는 아무 일도 일어나지 않는다** — 창구가 닫혀 있다. 그쪽에 최고관리자를 세우려면 `mbr_role_rel`에 한 줄을 직접 넣어야 한다.
    - 테스트에서 회원이 없는 상태를 전제하면 이 경로가 걸린다. `MemberControllerTest`가 `@BeforeEach`로 무관한 회원을 하나 두는 것은 그래서이며, 부트스트랩 자체는 `MemberSignupBootstrapTest`(API)와 `MemberSignupBootstrapConcurrencyTest`(경합, 전용 H2 DB)가 맡는다.
  - **역할↔권한 매핑은 운영 데이터이며 시드는 초기값일 뿐이다**(#65 · BR-M30). `AuthorityController`(`/v1/authorities`)와 `RoleAuthorityController`(`/v1/roles/{roleId}/authorities`)가 화면에서 바꾸는 길이고, 관리 로직은 `service/AuthorityAdminService(Impl)` 한 곳이다. 두 컨트롤러 모두 **클래스 레벨** `@RequireAuthority(ROLE_MANAGE)`다(VR-M12) — 조회도 예외가 아니다. 권한 부여·회수는 새 테이블 없이 기존 두 테이블만 쓰므로 `prod` DDL이 필요 없다.
    - **관리 서비스는 규칙을 새로 만들지 않는다.** 순환 검사는 `AuthorityEntity.changeParent`(조상 거슬러 올라가기, #9), 자손 펼침은 `AuthorityPolicy.expandOf`(#65에서 `capabilitiesOf`의 펼침을 목록 입력용으로 연 것)를 부르기만 한다. 관리 쪽에 같은 규칙을 한 벌 더 적으면 화면이 그리는 트리와 인가가 보는 트리가 갈린다.
    - **자기 잠금 방지는 '바꾼 뒤 다시 물어보기'이며 구현은 `service/RoleManageSelfLockGuard` 하나다**(VR-M13). 교체를 적용하고 `flush()` 한 다음 `AuthorityPolicy.hasAuthority(요청자, ROLE_MANAGE)`가 false면 409 `CANNOT_REVOKE_OWN_ROLE_MANAGE`로 던져 트랜잭션을 되돌린다. **인가를 좁히는 문은 둘이다** — 역할에서 권한을 회수하는 것(#65)과 사람에게서 역할을 떼는 것(#81 종료·기간 단축)이며, 결과가 같으므로 잠금 장치도 하나여야 한다. 판정을 복제하면 한쪽만 고쳐진 규칙이 다른 쪽에 남아 우회로가 된다. 요청자의 역할과 대상 역할을 비교해 미리 계산하면 '유효 기간·여러 역할·자손 펼침'을 여기서 한 벌 더 구현하게 된다. **묻는 시점은 '오늘'이 아니라 그 변경이 발효되는 날이다**(#110 · ssccops#86). 역할 종료는 종료일 **다음 날**에 발효되므로(BR-M25는 종료일 당일까지 유효로 본다) 오늘로 물으면 자기 ROLE_MANAGE 역할을 오늘·미래 날짜로 끝내는 조작이 "아직 가지고 있다"는 답을 받아 그대로 통과했다 — 실제로 막히던 것은 종료일이 어제 이하인 경우뿐이었고, 회귀 테스트도 그 값만 확인하고 있어 초록인 채로 결함이 살아 있었다. 그래서 `AuthorityPolicy`에 기준일을 받는 `capabilitiesOn`·`hasAuthorityOn`이 있고 가드가 `effectiveDate`를 받는다(권한 회수 #65는 즉시 발효라 종전대로 오늘이다). **발효 시점으로 묻는 것은 대상 배정이 요청자 본인의 것일 때뿐이다**(`MemberRoleAssignmentServiceImpl.selfLockBaseDate`) — 조건 없이 미래로 물으면 임기가 정해진 관리자가 **남의** 역할을 그보다 먼 종료일로 끝낼 때 자기 권한을 전혀 좁히지 않는데도 409가 난다. 대신 **이 규칙을 검증하는 테스트에는 `@Transactional`을 걸 수 없다** — 참여 트랜잭션은 rollback-only 표시만 되고 실제로 되돌아가지 않는다(`RoleAuthoritySelfLockTest`, `MemberSignupRollbackTest`와 같은 이유). 같은 이유로 트랜잭션을 건 컨트롤러 테스트는 **실패하는 요청 하나로 끝내야** 한다. 그 뒤 이어지는 요청은 `UnexpectedRollbackException`을 만난다.
    - `PUT /v1/roles/{roleId}/authorities`는 전체 교체이며 **차집합만 움직여 유지되는 부여의 `crt_dt`를 보존**한다(`FormLabelServiceImpl.replaceFormLabels`와 같은 방식). 응답은 직접 부여(`grants`)와 펼친 결과(`effectiveAuthrtCds`)를 함께 내린다 — 화면이 자손을 스스로 펼치면 그 계산이 서버와 갈린다.
    - `sys_yn = true`는 이름·설명·트리 위치는 바꿀 수 있고 **삭제와 코드 변경만** 막는다(BR-M33, 409 `SYSTEM_AUTHORITY_IMMUTABLE`). 사용자 정의 권한의 코드 변경은 409 `AUTHORITY_CODE_IMMUTABLE`로 갈린다 — 그쪽은 지우고 새로 만들면 되는 길이 있다. 삭제는 부여돼 있거나 **자식이 달려 있으면** 409 `AUTHORITY_IN_USE`다(자식을 조부모로 조용히 옮기면 그 순간 인가 범위가 바뀐다).
    - `PATCH /v1/authorities/{authrtCd}`는 메서드만 PATCH이고 **본문은 노드 한 벌 전체**다. JSON은 '필드 없음'과 'null'을 record로 구별할 수 없어 `upAuthrtCd`를 부분 수정으로 두면 "상위를 건드리지 마라"와 "최상위로 올려라"가 같은 요청이 된다. 생성은 `save()`가 아니라 `EntityManager.persist()`를 쓴다 — 코드가 직접 넣는 PK라 `save()`는 `merge()`로 돌고, merge는 행이 있으면 UPDATE로 넘어가 동시 생성의 진 쪽이 409가 아니라 남의 권한을 덮어쓴다.
    - `#9`의 `MemberErrorCode.AUTHORITY_PARENT_CYCLE`은 `AUTHORITY_CYCLE_DETECTED`로 이름과 코드 문자열이 바뀌었다(`VALIDATION_FAILED` → 전용 코드). #9 시점에는 API가 없어 도달할 수 없는 코드였고, #65에서 실제로 화면에 노출되는 조작이 됐다.
  - **CSV 명부 이관의 사전 검증**은 `POST /v1/members/imports/preview`·`/validation` 두 경로다(#84 · ssccops#75). 둘 다 클래스 레벨 `@RequireAuthority(MEMBER_MANAGE)`이고 **아무것도 쓰지 않는다**(`@Transactional(readOnly = true)`, 테스트가 mbr 행 수로 못 박는다) — 실제 등록은 별도 엔드포인트(#85)의 몫이다.
    - **CSV 파서는 `service/MemberImportParser` 한 곳뿐이다.** 미리보기를 서버에 둔 것이 그래서다 — 웹이 헤더를 따로 파싱하면 따옴표로 감싼 헤더(`"회장,프로젝트장"`)에서 해석이 갈려 화면에서 매핑한 컬럼과 서버가 읽는 컬럼이 어긋난다. 직접 split 하지 않고 `commons-csv`를 쓰며 BOM·줄바꿈 포함 필드·이스케이프된 따옴표를 처리한다. `rowNo`는 레코드 순번이 아니라 **원본의 물리적 줄 번호**(헤더 = 1행)다 — 운영자가 파일을 열어 찾는 것은 줄 번호이고, 줄바꿈을 포함한 필드가 있으면 두 값이 갈린다.
    - **학적 조건부 필수('재학이면 학번·학과·학년 필수')의 유일한 구현은 `service/AcademicProfilePolicy`다.** 원래 `MemberSignupRequest.isAcademicProfileComplete()`에만 있던 규칙을 꺼낸 것이며 가입·이관이 함께 쓴다(회원 정보 수정 #77도 여기서 꺼내 쓸 것). 복제하면 가입에서 막히는 값이 이관에서 통과해, 화면으로는 만들 수 없는 회원이 파일로 들어온다. 어떤 상태가 요구하는지는 `MemberStatusCode.requiresAcademicProfile()`이, 어떤 필드가 요구되는지는 이 정책이 갖는다.
    - **등급·상태 명칭('정회원'·'재학')을 자바 코드에 적지 않는다.** CSV에는 코드가 아니라 명칭으로 오며, `service/MemberImportReferenceData`가 `mbr_grd_nm`·`mbr_stts_nm`을 읽어 역매핑을 만든다 — 코드에 박으면 기준정보에서 이름을 바꾼 다음 날부터 명부가 통째로 거절된다(테스트가 이름을 바꿔 확인한다). 두 컬럼 모두 UNIQUE가 아니므로 명칭이 겹치면 표시 순번이 앞선 쪽을 고른다.
    - **`telno` 누락은 오류가 아니라 경고다.** 데이터사전이 NULL을 허용하므로 이관을 막을 근거가 없다 — 다만 계정 연결이 A안(학번+회원명+전화번호 3종 일치, ssccops#78)이라 연락처가 빈 회원은 나중에 스스로 연결하지 못하므로 `warnings`에 담아 이관 전에 채울 기회를 준다. 그 행은 여전히 `OK`이고 `okCount`에 들어간다. 요약의 `okCount`·`errorCount`·`duplicateCount`는 서로 겹치지 않으며 합이 `totalCount`이고, `warningCount`만 그 관계 밖이다 — 대신 **`warningCount ⊆ okCount`**다.
    - **경고는 `OK`인 행에만 싣는다**(#109, 결정 ssccops#85). 경고의 뜻이 "이관은 되지만 나중에 불편하다"이므로, 실행이 등록하지 않는 행(`ERROR` → `FAILED`, `DUPLICATE` → `SKIPPED`)에 붙으면 문장 자체가 거짓이고 운영자에게 소용없는 일을 시킨다. **중복을 오류와 함께 빼는 근거는 사유의 종류가 아니라 결과가 같다는 것**이다(BR-M40, 자동 병합 없음) — 기준을 '이 행이 실제로 이관되는가' 하나로 두면 규칙이 하나로 끝나고, '오류만 뺀다'로 두면 `duplicateCount`와 `warningCount`가 같은 행을 두 번 세는 문제가 남는다. 경고를 잃는 것이 아니라 미루는 것이다 — 사유를 고쳐 다시 검증하면 그 행은 `OK`가 되고 그때 경고가 나타난다. 거르는 자리는 `MemberImportValidator.warningsOf` **한 곳**이며 항목별 검사는 다른 항목의 판정을 모르는 채로 둔다(경고가 늘어도 만드는 쪽마다 상태를 확인할 필요가 없다).
    - 중복은 두 방향이다 — `mbr`에 이미 있는 학번과 **같은 CSV 안에서 겹치는 학번(두 행 모두)**. 보고만 하고 자동 병합은 없다(BR-M40). DB 조회는 `MemberRepository.findStudentNumbersIn`으로 학번 전체를 한 번에 모아 하며, 기준 데이터를 합쳐 행 수와 무관하게 3회다.
    - **졸업연도·역할은 매핑 대상에 없고**(BR-M42·BR-M44, `mbr`에 대응 컬럼이 없다) **`gen_no`를 학번으로 추정하지 않는다**(BR-M43, 미입력은 0). 매핑이 필수인 것은 `mbrNm`·`mbrGrdCd`·`mbrSttsCd` 셋이며 어기면 행별 오류가 아니라 400 `CSV_MAPPING_INVALID`다 — 매핑이 틀리면 모든 행이 같은 이유로 틀려 오류 목록만 남는다.
    - 파일 상한 5MB는 `MemberImportServiceImpl`이 끊는다. `spring.servlet.multipart.max-file-size`는 그보다 **높게**(16MB) 두는데, 서블릿 계층이 먼저 걸러 버리면 도메인 오류 코드(`INVALID_CSV_FILE`)가 붙지 않은 응답이 나가기 때문이다. 그래도 닿는 초대형 요청은 `GlobalExceptionHandler.handleMaxUploadSizeExceededException` **재정의**가 400으로 옮긴다 — `@ExceptionHandler`를 새로 달면 `ResponseEntityExceptionHandler`와 겹쳐 컨텍스트가 아예 뜨지 않는다.
  - **이관 회원이 계정을 붙이는 경로는 `POST /v1/members/link` 하나다** (#86 · 결정 ssccops#78 A안). CSV로 이관된 회원은 그전까지 가입 자체가 불가능했다 — 자기 학번을 넣으면 `STUDENT_NUMBER_DUPLICATED`로 끊기고, 빼고 가입하면 같은 사람이 두 행이 되며 명부의 이관 행은 로그인 없는 유령으로 남는다. 가입에 옵션을 더하지 않고 **별도 엔드포인트·별도 컨트롤러(`MemberLinkController`)**로 연 것은 성공 조건도 실패 처리도 다르기 때문이다 — 가입은 입력값 검증이지만 연결은 본인 확인이라 시도 횟수 제한이 붙는다. 가입과 같은 이유로 `@CurrentMember`가 아니라 `@AuthenticationPrincipal`을 쓴다(연결을 요청하는 사람은 정의상 아직 회원이 아니다). **`mbr` 행이 생기는 경로가 아니다** — `MemberEntity.assignAuthUserId`로 `auth_user_id` 한 컬럼만 채우며 기수·등급·상태·역할은 명부의 값 그대로다(BR-M51). 응답은 가입·세션 조회와 같은 `MemberProfileResponse`이고 새로 생긴 자원이 없으므로 201이 아니라 200이다.
    - 판정은 **학번·회원명·연락처 3종 완전 일치**이며 유일한 구현은 `service/MemberLinkPolicy`다. 연락처를 함께 요구하는 근거는 그 값이 `MEMBER_MANAGE` 없이는 조회되지 않는다는 사실이다(목록·상세가 그 권한으로 막히고 `/v1/members/assignable`은 연락처를 싣지 않는다) — 학번·이름과 달리 명부를 봤다는 것만으로는 알 수 없다. **비교는 정규화 후에** 한다(연락처는 숫자만 남기고, 회원명·학번은 앞뒤 공백을 지운다). 화면이 따로 정규화하면 규칙이 두 벌이 되어 화면과 API의 결과가 갈리므로 비교하는 쪽인 서버 한 곳에 둔다. **비어 있는 값은 어떤 입력과도 일치하지 않는다** — 연락처가 NULL인 이관 회원(#84가 경고로만 통과시킨 행)은 운영진이 값을 채우기 전까지 어떤 입력으로도 연결되지 않으며, 양쪽 다 NULL인 것을 일치로 보면 A안이 통째로 무너진다.
    - **실패는 404 `MEMBER_LINK_FAILED` 한 가지·한 문구다** (VR-M23). 항목별로 나누면 학번만 바꿔 가며 부르는 것으로 명부의 이름을 확인할 수 있어 그 자체가 명부 조회 도구가 된다. 3종을 다 맞혔는데 그 행에 이미 계정이 붙어 있는 경우만 409 `MEMBER_ALREADY_LINKED`로 갈라낸다 — 연락처까지 맞힌 사람이라 그 응답이 새로 알려 주는 사실이 없다. 이미 가입한 계정의 요청은 409 `ALREADY_SIGNED_UP`(기존 코드 재사용)이다.
    - **시도 횟수 제한은 `service/MemberLinkAttemptLimiter`**다 (VR-M24). 계정당 10분 안에 5회 실패하면 마지막 실패로부터 10분 잠그고, 잠금 판정은 **후보 조회보다 앞**이다(조회한 뒤 응답만 막으면 훑는 일 자체를 막지 못한다). IP당이 아니라 계정당인 근거와 인메모리 구현의 한계(인스턴스별 · 재기동 시 초기화 · 계정을 새로 만들면 초기화)는 그 클래스 주석에 있다 — 데이터사전에 시도 이력 테이블이 없어 새 테이블을 만들지 않았다.
    - **같은 행을 노리는 동시 요청은 `uk_mbr_auth_user_id`가 막지 못한다.** 그 제약이 막는 것은 '한 계정이 두 명부 행에 붙는 것'이고, 두 계정이 같은 행을 노리면 UPDATE가 한 컬럼을 덮어쓸 뿐이라 제약에 걸리지 않아 늦게 커밋한 쪽이 조용히 이긴다. 그래서 본인 확인을 통과한 뒤 그 행을 `PESSIMISTIC_WRITE`로 잠그고 `auth_user_id`를 **스칼라로 다시 읽어** 비어 있는지 확인한다(최초 가입자 부트스트랩 #71의 '잠그고 다시 센다'와 같은 두 단계이며, 후보 조회 단계에서는 잠그지 않으므로 실패하는 대다수 요청은 아무 행도 잠그지 않는다). 엔티티로 다시 조회하면 1차 캐시의 옛 값이 돌아오고, `auth_user_id IS NULL`을 WHERE에 넣으면 잠금 대기가 풀린 뒤 조건을 다시 평가하는지가 DB마다 갈린다. UNIQUE 위반도 409로 옮겨 최종 방어선으로 남긴다.
    - **연결 이력 테이블을 만들지 않았다.** 데이터사전에 없고, `mbr_stts_hstry`에 상태 변화 없는 행을 억지로 넣으면 이력의 뜻이 흐려진다 — 지금은 로그로만 남기며, 화면에 필요해지면 테이블 등재가 먼저다.
  - **회원에게 역할을 붙이는 경로는 `MemberRoleAssignmentController`(`/v1/members/{memberId}/roles`) 하나다**(#81). 그전까지 `mbr_role_rel`에 행이 생기는 곳은 최초 가입자 부트스트랩(#71)뿐이라 두 번째 회원부터는 어떤 권한도 받을 수 없었고, 인가 체계(#9)와 역할별 권한 부여(#65)가 열어 둔 길이 사람에게 닿지 않았다. **요구 권한은 `MEMBER_MANAGE`가 아니라 `ROLE_MANAGE`이며 클래스 레벨이다**(VR-M12, 조회도 예외가 아니다) — 표준코드가 `MEMBER_MANAGE`를 "회원 정보·등급·상태"로, `ROLE_MANAGE`를 인가 조작으로 갈라 놓았고, 회원 정보를 고칠 수 있다고 스스로에게 임원 역할을 붙일 수 있으면 `MEMBER_MANAGE`가 사실상 최고 권한이 된다. 새 테이블이 없어 `prod` DDL도 필요 없다.
    - **종료는 삭제가 아니다.** `DELETE`를 두지 않았고 어느 경로도 행을 지우지 않는다 — `role_end_ymd`를 채워 과거로 만들 뿐이다. 지우면 "언제까지 국장이었는가"가 사라져 역할 삭제 가드(#79 `ROLE_IN_USE`)가 지키려던 이력도 함께 없어진다. 목록은 기본이 전량이고 `current=true`일 때만 유효한 것으로 좁힌다(그 질의는 새로 만들지 않고 BR-M25를 이미 구현한 `findValidByMemberIds`를 쓴다).
    - **겹치는 기간에 같은 역할을 두 번 부여하지 않는다**(409 `ROLE_ALREADY_ASSIGNED`). 기간이 겹치지 않는 재임은 허용하며 그래야 임기가 두 행으로 남는다. 부여 요청이 종료일을 받지 않아 새 배정이 언제나 `[시작일, ∞)`이므로 겹침 판정은 '기존 배정이 새 시작일 이후까지 살아 있는가' 하나로 끝난다 — 조건을 양쪽으로 일반화하면 `:endDate is null` 분기가 필요해지고 그런 질의는 Hibernate가 타입을 추론하지 못한다.
    - **대표 역할(`rprs_role_yn`)은 회원당 유효한 것 중 최대 1건**이며 새로 지정하면 기존 대표를 같은 트랜잭션에서 내린다. 여전히 표시용이고 인가에 쓰지 않는다(BR-M26) — 이 API가 그 사실을 바꾸지 않는다. 이미 끝난 임기에 남아 있는 값은 건드리지 않는다.
    - 행 하나가 기준일에 유효한지는 `MemberRoleAssignmentEntity.isValidOn`이 갖는다(질의로 거르는 쪽은 Repository). 둘로 나뉜 것은 쓰임이 달라서다 — `current=false` 목록에는 종료된 배정도 실리므로 행마다 배지를 물어볼 곳이 따로 필요하다.
  - **승인·투표 자격도 권한이 준다**(#123, 직위 코드 `role.role_pstn_cd`·`RolePositionCode`·`AuthorizerRole`은 삭제됐다). 승인·반려는 유형이 지정한 **결재 권한**(`sub_work_type.autzr_authrt_cd` — `AuthorityCode.subWorkApprovers()`의 `SUB_WORK_APPROVE_*` 4종) 보유자만, 찬반 투표(OPS-015)는 **`APPROVAL_VOTE`** 보유자만 할 수 있으며, 둘 다 `ApprovalAuthorityPolicy`가 `AuthorityPolicy`의 펼침으로 판정한다. 자격 대상을 넓히고 좁히는 것은 배포가 아니라 역할별 권한 화면의 조작이다 — 부서별 국장(홍보국장 …)에게는 `SUB_WORK_APPROVE_DIRECTOR`를 부여한다.
    - 시드는 결재·투표 권한 5종을 **`APPROVAL` 그룹**(sys_yn = FALSE, `SUPER` 직속) 아래 매달고 회장~국원 역할에 직위 대응대로 부여한다(투표는 다섯 역할 전부). 그룹이 `SUPER` 직속이므로 **최고관리자는 결재·투표 자격을 자동 보유한다** — #118의 '시스템 관리와 조직 결재를 섞지 않는다'는 유지하지 않는다(ssccops#108). 프로젝트장·스터디장·사용자 정의 역할은 부여가 없어 이름과 무관하게 자격이 없다.
    - **표시명은 서버가 내린다.** 유형·상세·승인함 응답이 `authorizerAuthorityCode`와 함께 `authorizerAuthorityName`(`authrt_nm`, `AuthorityNameFinder` 경유)을 싣고, 유형 폼의 선택지는 `GET /v1/sub-work-types/authorizer-authorities`가 준다 — 권한 이름은 화면에서 바뀌는 데이터라 웹이 코드 → 이름 사전을 하드코딩하면(옛 `AUTZR_ROLE_NM`) 개명 즉시 어긋난다.
    - 직위 코드 시절 `PATCH /v1/roles/{roleId}`에 있던 **'빈 문자열 = 해제' 특례는 필드와 함께 사라졌다.** 역할 CRUD에는 자격 관련 필드가 없다 — 자격 부여·회수는 `PUT /v1/roles/{roleId}/authorities` 하나로 수렴한다.
- `domain/form` — 폼(지원서·신청서). 문항은 별도 테이블이 아니라 `form.qitem_cpst_cn`(JSONB) 한 컬럼에 들어 있다 — 폼마다 문항의 개수·유형·검증 규칙이 전부 달라 정규화해도 매번 전량 조회가 되기 때문이다. 그 대가로 **DB가 구조를 보장하지 않으므로 서버가 최종 방어선**이며, 그 검사를 모아 둔 곳이 `service/QuestionCompositionValidator`다(페이지 범위·`qitemId` 유일성·선택지·분기 대상 페이지 실재 여부·`Pattern.compile()` 성공 여부·최대 선택 수). 공개 폼 응답 검증(#35)이 같은 규칙을 반대 방향으로 쓰므로 규칙을 다른 곳에 복제하지 말 것. 유형과 무관한 잔여 속성(비선택형에 남은 `optionList` 등)은 거절이 아니라 **정리**하고, 유형에 해당하는 값이 잘못된 것만 400 `INVALID_QUESTION_COMPOSITION`으로 거절한다.
  - `qitemId`는 응답(`form_rspns_hstry.rspns_cn`)의 **key**다. 그래서 응답이 한 건이라도 있는 폼은 기존 `qitemId`의 삭제·변경을 409 `QUESTION_ITEM_IN_USE`로 막는다(문항 추가는 허용). 임시저장(DRAFT) 응답도 판단에 포함한다 — 목록의 `responseCount`가 DRAFT를 빼는 것과는 기준이 다르다.
  - **코드가 폼을 가리키는 열쇠는 `sys_form_cd`이며 `form_id`도 제목도 아니다**(#140). `form_id`는 IDENTITY라 환경마다 다르고 제목·라벨은 화면에서 바뀌는 운영 데이터다 — 승인자 판정이 역할'명'을 보던 동안 개명 한 번으로 승인자가 사라진 일이 있었다(#118 → #123). 조회 경로는 `FormRepository.findBySystemFormCode` 하나이며, `sys_form_cd`는 UNIQUE에 NULL 허용(평범한 폼)이다. **시스템 폼을 세우는 자리는 `FormEntity.designateAsSystemForm` 하나뿐**이고 요청 본문으로 지정하는 길은 없다 — 화면에서 붙일 수 있으면 코드가 기대하는 문항이 없는 폼이 시스템 폼이 되어 잠금이 지키는 것이 없어진다. **첫 시스템 폼은 기획안(`PROPOSAL`)이며 #173이 세웠다** — 기획안을 폼으로 받고 승인 시점에 학술 활동으로 이관한다는 결정(ssccops#131)이 #140 당시의 미결(ssccops#114)을 끝냈다. `findBySystemFormCode`를 부르는 코드는 그 시드(`service/ProposalFormSeeder`)이고, `requireDeletable`은 폼 삭제 API가 아직 없어 여전히 부르는 곳이 없다 — 삭제 경로가 생길 때 반드시 지나야 하는 자리로 남겨 둔다.
    - 잠금 범위는 `authrt.sys_yn` 선례 그대로다(BR-M33) — **삭제(409 `SYSTEM_FORM_IMMUTABLE`)와 코드 계약 위반(400 `SYSTEM_FORM_CONTRACT_VIOLATION`)만 막고** 제목·접수 기간·라벨·문구·문항 추가·순서 변경·상태 전이는 열어 둔다. 운영진이 회차마다 바꾸는 값이라 잠그면 한 번 세운 뒤 아무도 운영할 수 없는 폼이 된다. **폼 삭제 API는 아직 없다** — `FormEntity.requireDeletable`은 그 경로가 생길 때 반드시 지나야 하는 자리를 미리 정해 둔 것이며, 삭제를 만드는 이슈에서 잠금 판정을 새로 적으면 규칙이 두 벌이 된다.
    - 코드가 요구하는 `qitemId` 목록은 `service/SystemFormContract`가 선언한다(`sys_form_cd` → 필수 집합). 폼을 **읽는 쪽**이 여기에 적고 폼 저장 경로가 그 선언을 근거로 거절한다 — 요구 사항이 그 도메인 서비스 안에만 있으면 운영자가 문항을 지우는 순간 조용히 빈 값이 읽힌다. 표에는 `PROPOSAL` 한 줄이 있고(#173), 상수가 아니라 빈인 것은 테스트가 계약을 갈아 끼워 배선 전체를 검증하기 위해서다. 이 판정은 응답 유무를 보는 `QUESTION_ITEM_IN_USE`와 **기준이 다르다** — 응답이 한 건도 없어도 계약 문항은 지울 수 없다. **계약은 응답으로도 내린다**(#155) — 폼 상세(`GET /v1/forms/{formId}`)가 `systemRequiredQitemIds`(정렬된 문자열 배열, 시스템 폼이 아니면 빈 배열이고 `null`이 아니다)로 그 집합을 그대로 싣는다. 값은 `SystemFormContract.requiredQitemIdsOf` 하나에서 오고 조립하는 자리(`FormServiceImpl.getForm`)는 다시 계산하지 않는다 — 그전까지 웹(`ssccops-web#132`)은 저장이 400으로 터진 뒤 빠진 문항을 역산해 잠갔고, 그래서 잠금이 사후에만 걸렸으며 같은 사실의 출처가 서버 선언과 화면 추론 두 벌이 됐다(역할별 권한이 `grants`와 `effectiveAuthrtCds`를 함께 내리는 것과 같은 이유). 화면이 미리 잠그는 것은 편의이고 **서버의 400이 방어선이라 둘 중 하나를 없애지 않는다.** 목록(`FormSummaryResponse`)에는 싣지 않는다 — 문항 편집은 상세·편집 화면에서만 하고, 목록이 `qitemCpstCn`을 빼는 것과 같은 줄기다.
    - **기획안 폼(`PROPOSAL`) 시드는 `data.sql`이 아니라 자바다**(#173 · `service/ProposalFormSeed`+`ProposalFormSeeder`). 문항 구성이 JSONB라 **H2와 PostgreSQL이 같은 SQL 리터럴을 다르게 읽는다** — H2는 `FORMAT JSON` 절이 없으면 문자열을 JSON '문자열 값'으로 저장해 Hibernate가 다시 읽을 때 터지고, 그 절은 PostgreSQL이 값 표현식 자리에서 받지 않는다. `data-h2.sql`/`data-postgresql.sql`로 나누면 같은 JSON이 두 파일에 복사된다. 자바로 두면 덤으로 `qitemId`가 상수가 되어 시드·`SystemFormContract`·이관(#150)·테스트가 같은 값을 가리킨다. 멱등 판정은 `sys_form_cd` 하나이고 **이미 있으면 UPDATE로 덮어쓰지 않는다**(운영진이 손댄 문구·문항·상태를 배포가 되돌리면 잠금을 삭제·계약 위반으로만 좁혀 둔 것이 무의미해진다). **`form_stts_cd`는 `DRAFT`이고 접수 기간은 비운다** — 접수를 여는 것은 운영진의 조작이지 배포의 부작용이 아니다. `mltpl_rspns_yn = TRUE`(한 사람이 스터디·프로젝트를 각각 낸다, #143)이고 라벨 `기획안`을 함께 세운다(표시용이며 코드가 찾는 열쇠는 여전히 `sys_form_cd`다). **회원이 한 명도 없으면 세우지 않는다** — `form.creatr_mbr_id`가 NOT NULL이라 명의가 필요한데, 컬럼을 NULL 허용으로 열면 관리자 목록 질의의 `join fetch f.creator`가 그 폼을 통째로 빼 버려 세우자마자 보이지 않는 폼이 되고, 제약 변경이라 `ddl-auto: update`가 반영하지도 않는다. 생성자는 `MemberRepository.findTopByOrderByIdAsc`(최초 가입자)다. **그래서 시드가 도는 자리가 둘이다**(#184): 기동(`ApplicationRunner`)과 **회원 생성 커밋 직후**(`MemberCreatedEvent` → `onMemberCreated`). 기동만으로는 회원이 생겨도 다음 기동까지 폼이 없는데, `local`은 `create-drop`이라 그 '다음 기동'이 방금 만든 회원을 함께 지워 **어떤 순서로도 폼이 서지 못했다**. 기동 경로를 남겨 두는 것은 회원은 있는데 폼이 없는 기존 DB 때문이다(그쪽에는 가입 이벤트가 오지 않는다). 이벤트를 서비스가 아니라 `MemberEntity`의 `@DomainEvents`로 내는 것은 회원 생성 경로가 둘이고(가입·명부 이관) 앞으로 늘 수 있어서다 — 둘 다 `memberRepository.save*`를 지나므로 저장소 한 지점에서 잡힌다(발행 판정이 메서드명 `save` 접두사라 `saveAndFlush`도 포함된다). 리스너는 `AFTER_COMMIT` + `TransactionTemplate`(`REQUIRES_NEW`)이며 예외를 **트랜잭션 경계 바깥에서** 잡는다 — `AFTER_COMMIT` 리스너의 예외는 삼켜지지 않고 `commit()`을 부른 쪽으로 올라가, 데이터는 커밋됐는데 가입 API만 500이 되는 자리다.
      - `qitemId` 11종은 **응답(`rspns_cn`)의 key라 접수가 한 건이라도 있으면 바꿀 수 없다**: `programType`(유형 → `acdm_actv_type_cd`) · `programTitle`(활동명 → `event.event_ttl`) · `goalContent`(`goal_cn`) · `prepContent`(`prep_cn`) · `periodBeginDate`/`periodEndDate`(`event.event_bgng_dt`/`event_end_dt`) · `scheduleText`(`schdl_cn`) · `capacityMinCount`/`capacityMaxCount`(`pscp_min_cnt`/`pscp_max_cnt`) · `placeName`(`event.plc_nm`) · `curriculum`(`crclm_artcl[]`). 컬럼명이 아니라 뜻을 적은 것은 학술 도메인이 컬럼을 개명해도 응답 key는 바꿀 수 없기 때문이다. **정원 두 문항은 `SHORT_TEXT` + 정규식 `^[0-9]+$`다** — `QuestionItemType`에 숫자 유형이 없고, 그 어휘는 웹 `codes.ts`의 `QitemTypeCd`와 같아야 하는 계약이라 서버만 먼저 늘리면 편집기가 그릴 수 없는 유형이 폼에 들어간다.
      - **`SystemFormContract`가 잠그는 것은 여섯 개뿐이다**(`programType`·`programTitle`·`goalContent`·`periodBeginDate`·`periodEndDate`·`curriculum`). 기준은 '이관이 읽는가'가 아니라 **'없으면 이관이 성립하지 않는가'**다 — 선택 문항은 #150이 읽기는 하지만 비어 있어도 되는 값이라 문항을 지운 것과 제출자가 비워 둔 것의 결과가 같고, 잠그면 이득 없이 운영진이 회차마다 폼을 다듬을 여지만 없앤다.
      - **커리큘럼 줄 포맷은 `1회차 | 주제 | 2026-03-05`이며 날짜는 생략 가능하다**(`ProposalFormSeed.CURRICULUM_LINE_FORMAT`). 이 문구가 문항 문구(`qitemLblNm`)에 그대로 들어간다 — **제출자가 읽는 안내가 곧 파서의 명세**라 두 곳에 따로 적으면 반드시 갈리고, 갈린 순간 제출자는 안내대로 적었는데 승인이 막힌다. 구분자가 `|`인 것은 주제에 쉼표·하이픈·콜론이 흔히 들어가서다. **정규식(`ptrnCn`)은 걸지 않는다** — 접수 단계에서 구조를 강제하지 않는 것은 확정된 결정이고(ssccops#131) 구조화는 승인 시점(#150)에 한다.
      - 유형 선택지는 `acdm_actv_type.type_nm`(현재 `스터디`·`프로젝트`)과 **글자까지 같아야 한다** — 응답이 문자열이고 이관이 그것을 코드로 되돌리기 때문이다. 시드가 기준정보를 조회해 만들지 않는 것은 문항 구성이 기동 시점의 DB 상태에 따라 달라지지 않게 하기 위해서이며, 두 값이 같은지는 `ProposalFormSeedTest`가 못 박는다(기준정보에서 이름을 바꾸면 그 테스트가 빨개진다).
    - 복제본은 `sys_form_cd = null` · `sys_yn = false` · `qitem_ver = 1`이다. 해제 코드를 복제 쪽에 적지 않고 `FormEntity.create`가 언제나 그 상태로 만들게 둔다 — 새 폼을 만드는 경로가 늘 때마다 해제를 다시 적어야 하는 것을 피한다.
  - **문항 구성 버전(`form.qitem_ver`)은 구성이 실제로 바뀐 저장에서만 오른다**(#140). 편집 자동 저장(ssccops #63)이 매 타이핑마다 PUT을 쏘므로 무조건 올리면 버전이 수백까지 뛴다. 비교는 `QuestionCompositionContent`의 record equals이고 대상은 **검증기가 정규화한 뒤의 값**이다(정규화 전과 비교하면 잔여 속성이 정리된 것만으로 버전이 오른다). 올랐는지는 `FormEntity.update`가 boolean으로 돌려주며 서비스는 그 값만 보고 이력을 남긴다 — 서비스가 구성을 한 번 더 비교하면 같은 규칙이 두 벌이 되어 버전과 이력이 갈린다.
    - 버전이 오를 때마다 `form_qitem_hstry`에 그 시점 구성을 통째로 남긴다. 생성·복제도 `qitem_ver = 1`로 한 행을 남기며(수정에서만 남기면 1번의 내용만 어디에도 없다) 세 경로가 모두 `FormServiceImpl.recordQuestionComposition` 한 자리를 지난다. 변경자(`chnrg_mbr_id`)는 요청 본문이 아니라 `@CurrentMember`에서 온다(#78과 같은 규칙) — 그래서 `PUT /v1/forms/{formId}`가 #140부터 주체를 받는다. 이력 행은 전 컬럼 `updatable = false`이고 `(form_id, qitem_ver)`가 UNIQUE다.
    - 응답 저장은 그 시점의 버전을 `form_rspns_hstry.qitem_ver`에 찍는다. 답이 갱신될 때마다 다시 찍는다(마지막에 쓴 답은 마지막에 본 구성에 대한 답이다) — 심사(`changeStatus`)에서는 찍지 않는다. **옛 버전 구성으로 다시 렌더하는 것은 범위 밖이다** — 응답 표시가 `qitemId` 기준이라 대체로 동작하고, 이력이 남아 있으므로 필요해지면 그때 연다.
  - 폼 목록(`GET /v1/forms`)은 `qitemCpstCn`을 싣지 않는다(상세·목록 모두 `sysFormCd`·`sysYn`·`qitemVer`는 싣는다 — 상세로 들어가기 전에 잠금 배지를 그릴 수 있어야 한다). 라벨은 `FormEntity`에 컬렉션 연관을 열지 않고 `FormLabelRelationRepository.findAllByFormIdIn`으로 한 번에 모아 오며, 응답 건수는 `FormResponseCount` 집계 프로젝션을 쓴다(폼 1 + 라벨 1 + 집계 1, 총 3회). 상태 필터는 NULL 비교 대신 **전체 상태 집합**을 넘긴다 — 열거형 파라미터에 NULL을 넣고 `:status is null`로 분기하면 Hibernate가 타입을 추론하지 못한다.
  - 라벨 지정 교체는 통째로 지우고 다시 넣지 않고 **차집합만** 움직인다. 같은 `(form_id, form_lbl_id)` 쌍을 한 트랜잭션에서 지웠다 넣으면 Hibernate가 INSERT를 DELETE보다 먼저 흘려보내 UNIQUE 제약에 걸린다.
  - 상태(`form_stts_cd`)를 바꾸는 길은 **`POST /v1/forms/{formId}/status` 하나뿐**이다(#33). `PUT /v1/forms/{formId}`는 본문에 `formSttsCd`가 실려 와도 **무시한다** — 편집 자동 저장(ssccops #63)이 상세 응답을 초안으로 받아 그대로 되돌려 보내므로 그 본문에는 늘 현재 상태가 실려 있고, 받아 쓰면 타이핑 한 번이 접수 상태를 덮어쓴다. 거절하지 않고 무시하는 것은 거절하면 자동 저장이 통째로 멈추기 때문이다. `FormSaveRequest.formSttsCd`는 생성(POST)에서만 쓰인다('바로 접수 시작'). `labelIds` 생략은 **전부 떼기**로 갈린다.
  - 전이표는 `FormStatusAction`(액션 → 대상 상태 + 허용 진입 상태)이 갖고, 전이 가능 여부와 사전 검증은 `FormEntity.changeStatus`가 던진다 — DRAFT→OPEN·OPEN→CLOSED·CLOSED→OPEN(마감 철회)만 허용하고 나머지는 400 `INVALID_FORM_STATUS_TRANSITION`. 문항 0개인 폼을 여는 것은 400 `FORM_HAS_NO_QUESTION`이며, 이 검증은 `FormEntity.create(..., OPEN)`(바로 접수 시작)에도 같이 걸린다 — 같은 결과(열린 폼)에 도달하는 두 경로가 다른 규칙을 쓰면 안 된다. 상태 전이 이력 테이블은 만들지 않는다(데이터사전에 없음, 감사 로그 #8이 확정되면 그쪽에 얹는다).
  - **"지금 이 폼이 응답을 받을 수 있는가"의 유일한 구현은 `service/FormReceiptPolicy`다**(#33). `form_stts_cd == OPEN && (rcpt_bgng_dt == null || now >= rcpt_bgng_dt) && (rcpt_end_dt == null || now <= rcpt_end_dt)`이며 경계는 양쪽 모두 포함, NULL은 '제한 없음'이다. `now`는 주입된 `Clock`(`global/config/ClockConfig`)에서 온다. 공개 폼 응답 제출(#35)·응답 자동 저장(#36)은 이 판정을 다시 구현하지 말고 `isAcceptingResponses(form)`만 호출한다.
  - 응답자용 공개 경로는 `GET /v1/forms/{formId}/public`·`POST /v1/forms/{formId}/responses`(#35)이며 운영자용 `FormController`와 컨트롤러·응답 스키마를 **분리한다**(`PublicFormController`·`PublicFormResponse`) — 응답자에게 `creatrMbrId`·응답 집계·`formSttsCd`를 줄 이유가 없고, 한 응답에 합치면 운영자용 필드가 늘 때마다 공개 링크로 새어 나갈 것이 함께 는다. **'공개'는 누구나 링크를 열 수 있다는 뜻이지 익명 제출이 아니다** — 두 경로 모두 인증이 필요하고 등급 제한은 없다(임시회원도 응답한다, ssccops #61). 접수 불가면 문항을 뺀 200이 아니라 **409 `FORM_NOT_ACCEPTING`**으로 끊는다(DRAFT·CLOSED·기간 전·기간 후를 한 코드로 묶는다) — 문항을 실을지 말지를 DTO 조립의 분기 하나에 맡기면 DRAFT 폼의 문항이 링크만으로 새어 나가는 데 그 분기 하나면 충분하다.
  - 제출된 답의 재검증은 `service/ResponseAnswerValidator`가 갖는다(#35, `QuestionCompositionValidator`의 짝). 유형별 어휘(`CHOICE_TYPES`·`TEXT_TYPES`·`BRANCHABLE_TYPES`)는 두 클래스가 나눠 쓰므로 복제하지 말 것. **분기(`branchMap`)로 건너뛴 페이지의 필수 문항은 필수가 아니다** — 제출된 답으로 웹 `nextTarget()`과 같은 이동을 되짚어 도달한 페이지 집합을 재현하고 그 밖은 필수 검사에서 뺀다(이 규칙이 없으면 분기 폼은 어떤 답으로도 제출되지 않는다). 정규식은 `matches()`가 아니라 `find()`로 본다(웹의 `new RegExp().test()`와 같은 뜻이어야 한다). 빈 값(`""`·`[]`)인 key는 저장하지 않고, 폼에 없는 `qitemId`는 조용히 버리지 않고 400이다. 저장 형태는 다중선택만 배열이고 나머지는 문자열이라, 웹이 배열로 보내는 단일선택은 서버가 벗겨 굳힌다.
  - 응답 자동 저장(#36)은 `GET`·`PUT /v1/forms/{formId}/responses/draft` 두 경로다. **경로에 `mbrId`를 두지 않는다** — 대상은 언제나 인증 주체 본인이며, 자리를 만들지 않는 것이 남의 초안에 닿는 경로를 막는 방법이다. `PUT`은 upsert이자 통째로 덮어쓰기이며(본문에 없는 답은 지운 것으로 본다), 행이 있으면 `rspns_cn`만 갈고 없으면 `DRAFT`·`sbmsn_dt = NULL`로 만든다. 상태와 제출 일시를 함께 채우는 자리는 제출(`submitResponse`) 하나뿐이다. 작성 중인 응답이 없으면 204가 아니라 **`data`가 null인 200**이다 — 모든 응답이 `ApiResponse` 봉투를 쓰는데 204만 본문이 없으면 웹의 공통 응답 처리가 이 엔드포인트 하나를 예외로 다뤄야 한다. 이미 제출한 경우도 같은 빈 응답이다(제출 여부는 공개 폼 조회의 `alreadySubmitted`가 전한다). 조회도 저장과 같이 `FormReceiptPolicy`를 태운다 — 한쪽만 409면 복원은 되는데 제출은 안 되는 화면이 생긴다.
  - **자동 저장은 검증하지 않는다.** 나누는 기준은 완성도인가 모양인가다 — 필수·정규식·최대 선택 수·선택지 실재 여부는 제출에서만 보고(작성 중에 어긋나 있는 것이 정상이라, 걸면 다 채우기 전까지 아무것도 저장되지 않는다), 폼에 없는 `qitemId`·저장 형태(다중선택은 배열, 나머지는 문자열)·크기는 자동 저장에서도 본다. 두 경로는 `ResponseAnswerValidator.validate`(제출) / `validateDraft`(자동 저장)로 갈리며 정리 규칙은 공유한다 — 복제하면 자동 저장으로 만든 행이 제출에서 다른 모양으로 다시 굳어 복원한 화면과 제출된 답이 어긋난다. `rspns_cn`은 답 전체 글자 수 10만 자가 상한이다(413 `RESPONSE_CONTENT_TOO_LARGE`, 제출에도 같이 건다) — 자동 저장은 타이핑마다 같은 본문을 통째로 다시 보내므로 상한이 없으면 붙여넣기 한 번이 행 하나를 무한정 키운다. 디바운스는 웹 책임이다.
  - **DRAFT는 응답 집계에 잡히지 않는다.** 목록(`GET /v1/forms`)의 `responseCount`·폼 상세의 응답 건수와 요약은 `ResponseStatus.submittedOrLater()`(SUBMITTED·CHANGES_REQUESTED·ACCEPTED·REJECTED — 수정요청은 #141에서 들어왔다. 심사가 끝나지 않았을 뿐 응답자는 이미 냈고, 빼면 수정요청을 누르는 순간 그 응답이 목록에서도 요약에서도 사라진다)만 센다. 빠뜨리면 "응답 3건"인데 목록에는 1건만 보이는 상태가 되고 마감 판단이 흔들린다. 원래 `FormServiceImpl`의 private 상수였는데 #37이 같은 집합을 쓰면서 코드 enum으로 옮겼다 — 서비스 두 곳에 같은 `EnumSet`이 놓이면 갈린다. 반대로 문항 식별자 보호(`existsByForm`)는 DRAFT를 **포함한다** — 기준이 다르다는 것을 기억할 것.
  - 운영자용 응답 조회·심사(#37)는 `FormResponseController`(`/v1/forms/{formId}/responses`)이고 서비스는 응답자 경로와 같은 `FormResponseService`를 쓴다 — 컨트롤러를 나누는 것은 무엇이 밖으로 나가는가의 문제이고, `form_rspns_hstry` 한 행을 다루는 규칙("DRAFT는 심사 대상이 아니다"·"제출은 응답자만 한다")은 서비스가 둘이 되는 순간 두 벌이 된다. 경로 접두사가 `PublicFormController`와 겹치는 자리가 셋 있다: `POST .../responses`(응답자 제출) vs `GET .../responses`(운영자 목록), `GET .../responses/draft`(#36)와 `GET .../responses/mine`(#143) vs `GET .../responses/{formRspnsId}` — 스프링이 메서드와 리터럴 세그먼트 우선순위로 갈라 주므로 충돌하지 않지만 어느 파일을 고칠지 헷갈리기 쉽다.
  - **목록의 기본값은 '전체'가 아니라 'DRAFT를 뺀 전부'다.** `statusCode=DRAFT`를 명시했을 때만 작성 중 응답이 나온다. 정렬은 `coalesce(sbmsn_dt, mdfcn_dt) desc, form_rspns_id desc` — DRAFT는 `sbmsn_dt`가 NULL이라 폴백이 필요하고, 동률을 식별자로 끊어야 상세의 이전/다음 이동이 목록과 같은 순서를 본다. 회원 정보(회원_명·학번·학과·등급·상태)는 응답에 복사하지 않고 `mbr`에서 조인하며, 목록은 `@EntityGraph`로 `member`·`member.membershipGrade`·`member.membershipStatus`까지 한 번에 끌어온다(폼 1 + 목록 1, 응답이 몇 건이든 2회 — 테스트가 못 박아 둔다). 응답 내용(`rspnsCn`)과 연락처(`telno`)는 목록에 싣지 않는다.
  - **페이징을 두지 않는다** (#37 결정). 목록을 한 번 받아 상태별로 걸러 보며 심사하는 화면이고 상세의 이전/다음도 그 순서를 그대로 쓰는데, 페이지를 나누면 '이전'이 경계에서 끊기거나 서버가 페이지 밖의 이웃까지 계산해야 한다. `ssccops-web #13`도 배열을 받는 전제로 이미 머지됐다(폼 목록 #32이 페이징을 미룬 것과 같은 이유). 실제로 느려지면 커서 기반으로 넣되 웹의 목록·상세 이동과 함께 바꾼다.
  - **폼 범위 검사는 조회와 쓰기 양쪽에 건다.** 단건 조회·상태 변경은 `findByIdAndForm(id, form)` 하나로만 찾는다 — 경로에 두 식별자가 다 있는데 응답 식별자만 보면 `/v1/forms/1/responses/999`가 다른 폼의 지원서와 개인정보를 그대로 돌려준다. 없는 응답과 남의 폼 응답은 같은 404 `FORM_RESPONSE_NOT_FOUND`다(코드를 나누면 그 폼에 그 번호가 있는지가 새어 나간다).
  - 응답 상태 전이표는 `FormResponseHistoryEntity.changeStatus`가 갖는다(폼 쪽 `FormEntity.changeStatus`와 같은 자리). **#141에서 어휘가 늘고 표가 좁아졌다** — 심사가 열려 있는 상태는 `SUBMITTED`·`CHANGES_REQUESTED` 둘뿐이고 그 둘에서 세 결론(`ACCEPTED`·`CHANGES_REQUESTED`·`REJECTED`) 중 하나로 갈 수 있으며, **결론을 낸 뒤(`ACCEPTED`·`REJECTED`)에는 아무 데로도 못 간다**. 어기면 400 `INVALID_RESPONSE_STATUS_TRANSITION`이며 DRAFT가 얽힌 전이·같은 상태로의 재지정·검토로 `SUBMITTED`로 되돌리기도 같은 코드다.
    - **#37의 '심사 번복 허용'을 뒤집은 것이다.** 승인 직후 후속 처리가 시작되므로(기획안이 승인되면 활동이 개설되고 역할이 부여된다) 뒤늦게 반려로 되돌리면 이미 만들어진 것들을 되돌릴 방법이 없다. 종결을 '응답자가 다시 낼 수 없다'로만 좁혀 두면 검토자 쪽에 그 구멍이 그대로 남는다. 오조작의 탈출구는 번복이 아니라 새 응답이다. 같은 상태로의 재지정도 이제 400인데, #37이 통과시킨 근거("아무것도 바꾸지 않을 뿐 잘못된 요청은 아니다")가 이력이 생기면서 무너졌기 때문이다 — 검토도 처리 이력 한 줄을 남기는 '사건'이 됐고, 통과시키면 아무것도 바꾸지 않은 처리가 타임라인에 쌓여 실제 심사 시점을 못 찾는다(`NO_CHANGE` #78과 같은 이유).
    - `SUBMITTED`로 가는 길은 **응답자의 제출·재제출(`submit`)뿐**이다. `DRAFT → SUBMITTED`가 막히는 것과 같은 이유가 `CHANGES_REQUESTED → SUBMITTED`에도 걸린다 — 검토자가 상태만 되돌려 놓으면 내용은 그대로인데 회차만 오르지 않은 '새 제출'이 생긴다.
    - 심사에는 `FormReceiptPolicy`를 태우지 않는다 — 접수가 끝난 뒤에 하는 일이라 걸면 마감한 폼의 응답을 아무도 승인할 수 없다. **재제출도 그 판정을 타지 않는다**(#177) — 접수를 마감한 뒤 검토하는 것이 정상 순서인 폼(기획안)에서는 마감 후에 수정요청이 나가고, 그때 재제출까지 막으면 응답자에게 다시 낼 길이 없다. 예외는 재제출 하나뿐이며 새 응답 제출(초안을 내는 것 포함)은 종전대로 판정을 탄다.
  - **상태 이력 테이블을 만들었다** (#141 · `form_rspns_rvw_hstry` · `FormResponseReviewHistoryEntity`). #37에서는 데이터사전에 없다는 이유로 만들지 않았고 `mdfcn_dt`만 갱신됐다 — "누가 승인했는지"도 "왜 반려했는지"도 어디에도 없었으므로 컨트롤러가 받은 `@CurrentMember`를 서비스로 넘기지도 않았다(넘기면 기록되는 것처럼 읽힌다). 그 판단을 뒤집는다: 수정요청·반려는 사유 없이 성립하지 않고, 사유는 처리자·시점과 함께여야 증거가 된다.
    - **이제 `@CurrentMember`는 서비스로 넘어가 `prcs_mbr_id`에 기록된다.** 처리자를 요청 본문으로 받지 않는 것은 #78이 세운 규칙과 같다 — 받아 주면 "누가 했는가"를 스스로 적어 넣을 수 있어 이력이 증거가 되지 못한다.
    - **사유를 응답 행의 컬럼으로 두지 않는다.** `form_rspns_hstry`에 `rjct_rsn` 하나를 더하는 쪽이 짧지만 재검토 때 직전 사유가 덮여 사라지는데, 화면이 보여주는 것은 마지막 사유가 아니라 처리 내역 **전부**다(수정요청 → 재제출 → 반려로 여러 번 오간 응답이 실제 대상이다). `sub_work_rjct`가 같은 이유로 별도 테이블이다.
    - 행은 지우지 않고 모든 컬럼이 `updatable = false`다(POL-004 · `mbr_grd_hstry`와 같은 잠금). 처리자_명은 복사하지 않고 `mbr`에서 조인하며(이름이 바뀌면 이력도 함께 바뀌는 것이 맞다) N+1은 `@EntityGraph`로 막는다.
    - **제출(`SUBMIT`)도 이 테이블에 쌓인다.** 검토 이력인데 제출이 들어 있는 것은 타임라인이 "제출 → 수정요청 → 재제출 → 승인"으로 읽혀야 하기 때문이다 — 제출 행이 빠지면 회차가 언제 올라갔는지가 사라져 승인 의견이 어느 제출본을 보고 쓴 것인지 알 수 없다. 그 행의 처리자는 검토자가 아니라 응답자다.
  - **검토 처리는 `POST /v1/forms/{formId}/responses/{formRspnsId}/reviews` 하나다** (#141). `PATCH .../status`를 대체했다 — 웹에서 이것은 시트 하나에 결과와 의견을 적고 저장을 누르는 **한 번의 조작**이라 경로도 하나여야 하고, 두 경로로 나누면 상태는 바뀌었는데 사유가 없는 응답이 남는데 이력 행이 잠겨 있어 나중에 채울 방법도 없다. 상태 한 필드를 고치는 요청이 아니라 '검토 처리'라는 사건을 남기는 요청이 됐으므로 PATCH가 아니라 POST이며, 새 자원의 URL을 돌려주지 않으므로 201이 아니라 200이다(`POST /v1/forms/{formId}/status`와 같은 모양).
    - 본문은 `rspnsSttsCd`(대상 상태) + `rvwOpnnCn`(검토 의견)이다. 액션이 아니라 상태를 받는 것은 검토자가 고르는 세 결론이 상태와 1:1이라 액션 어휘가 상태 어휘를 그대로 베낀 것이 되기 때문이다(폼 상태 전이가 액션을 받는 것과 갈리는 지점 — 그쪽은 '연다/닫는다'가 현재 상태에 따라 다른 결과로 간다). 처리 구분(`prcs_se_cd`)은 상태에서 유도한다.
    - **수정요청·반려는 검토 의견이 필수이고 승인은 선택이다**(400 `REVIEW_OPINION_REQUIRED`, 공백만 있는 문자열도 같다). 필요 여부의 판단은 `ResponseReviewAction.requiresOpinion`이, 거절은 `FormResponseReviewHistoryEntity.record`가 갖는다 — 서비스에 옮겨 적으면 검토를 기록하는 경로가 늘 때마다 규칙이 복제된다. `@NotBlank`으로 DTO에서 막지 않는 것은 필수 여부가 함께 온 상태 값에 달려 있고, 조건부 검증을 Bean Validation으로 표현하면 `VALIDATION_FAILED`로 뭉개져 웹이 "의견을 적으라"는 안내를 고를 수 없기 때문이다.
    - **상태 변경과 이력 INSERT는 한 트랜잭션이다** — 이력 저장이 실패하면 상태도 되돌아간다(`FormResponseReviewRollbackTest`, #78의 `MemberChangeRollbackTest`와 같은 자리). 그 테스트에 `@Transactional`을 걸 수 없는 이유도 같다.
  - **승인 후속 처리는 훅으로 넘긴다** (#150 · `service/SystemFormApprovalHook`+`SystemFormApprovalHooks`). 시스템 폼 응답이 `ACCEPTED`에 도달하면 `FormResponseServiceImpl.reviewResponse`가 `sys_form_cd`로 훅을 찾아 `onAccepted(response)`를 부르고, **누가 구현하는지는 모른다** — 기획안(`PROPOSAL`)의 구현체는 학술 도메인(`ProposalApprovalHook`)에 있다. 폼이 학술 서비스를 직접 주입받는 쪽이 짧지만 그러면 시스템 폼이 하나 늘 때마다 이 서비스에 도메인 이름이 하나씩 박힌다(회원 도메인이 `SubWorkService.countOngoingByOwner` 하나만 아는 것과 같은 규칙). 스프링 이벤트를 쓰지 않은 것은 **승인과 후속 처리가 한 트랜잭션이어야 하는데**(이관이 실패하면 `ACCEPT` 자체가 롤백된다) 리스너 예외가 발행자에게 도달하는지가 설정에 달려 있어 그 원자성이 설정 한 줄로 조용히 깨지기 때문이다. 같은 `sys_form_cd`에 훅이 둘이면 기동이 실패한다(두 도메인이 같은 승인에 반응하는 것은 런타임 선택지가 아니라 설계가 어긋난 상태다). 인터페이스를 #141이 아니라 이 이슈가 만든 것은, 구현체 없는 인터페이스만 먼저 두면 무엇을 위한 모양인지 알 수 없어 추측으로 굳기 때문이다.
    - **응답 상세가 `academicProgramPreview`를 함께 내린다**(`sys_form_cd = 'PROPOSAL'`일 때만, 그 밖에는 `null`). `{typeCd, curriculumItems[{seqno, ttl, planYmd}], migratable, failureReason}`이며 **실제 이관과 같은 파서**(`ProposalResponseParser`)가 만든다 — 커리큘럼은 자유 텍스트로 접수되므로(정규식을 걸지 않는다) 검토 화면이 자기 파싱을 따로 하면 검토자가 승인한 것과 서버가 만드는 것이 갈린다(ssccops#148 BR). 필드 타입이 `Object`인 것은 폼이 그 모양을 알아서는 안 되기 때문이고, 실제 형태는 학술 도메인의 `AcademicProgramPreviewResponse`가 정한다. **`preview`는 예외를 던지지 않는다** — 파싱에 실패한 응답이야말로 검토자가 사유를 봐야 하는 응답인데 상세 조회가 500이면 그 화면을 열 수조차 없다. 실패는 `migratable = false` + `failureReason`으로 실리며 그 문장이 승인 시 나올 400의 `message`와 같다.
    - **이관 자체는 학술 도메인이다** (#150 · `domain/academicprogram/service/AcademicProgramMigrationService`). `Event` + `AcademicProgram` + `CurriculumItem[]`을 만들고 곧바로 #133의 `AcademicProgramApprovalEffectsService`(리더 역할·빈 모집 폼)를 잇는다 — 전부 한 트랜잭션이고 `@Transactional`은 REQUIRED다(`REQUIRES_NEW`로 열면 이관만 커밋되고 승인이 롤백되는 경로가 생긴다). 파싱 실패·유형 매핑 실패는 **승인 실패**이며 400 `PROPOSAL_MIGRATION_FAILED` + 사유다(`ProposalMigrationRollbackTest`가 못 박는다 — 그 테스트에 `@Transactional`을 걸 수 없는 이유는 검토 이력 롤백 테스트와 같다). 사유를 값으로 싣기 위해 `GeneralException`에 `detail`이 생겼다(있으면 `ErrorCode.message` 대신 그것이 나가고 `code`는 그대로다) — 사유마다 에러 코드를 만들지 않은 것은 "커리큘럼 3번째 줄"까지 담아야 쓸모가 있는데 그것은 코드가 아니라 값이기 때문이다. **역방향 동기화는 없다** — `acdm_actv.form_rspns_id`(NOT NULL·UNIQUE)는 출처 기록이지 연결이 아니며, 이관 뒤 응답을 고쳐도 활동은 바뀌지 않는다(승인이 종결이라 안전하다). 중복 이관은 선조회 + UNIQUE 두 겹으로 막고 409 `PROPOSAL_ALREADY_MIGRATED`다(정상 흐름에서는 도달할 수 없다 — `ACCEPTED`는 종결이다).
      - 매핑 규칙 중 코드가 정하는 것 셋: **유형은 `acdm_actv_type.type_nm` 조회로 되찾는다**(상수 맵을 두면 세미나 유형 하나를 여는 데 서버 배포가 필요해진다. `use_yn`은 보지 않는다 — 제출자는 접수 시점의 선택지를 골랐다). **행사 분류는 `EVENT` 고정**이다(학술 전용 분류를 새로 시드하면 스터디·프로젝트 어휘가 `acdm_actv_type`과 두 벌이 된다. `PROJECT` 분류만 있고 스터디에 대응하는 것이 없어 유형별 분기는 절반만 맞는 매핑이 된다 — 만들어진 뒤 운영진이 바꾸는 것은 정상이다). **행사 기간의 종료는 종료일 23:59:59**다(자정으로 두면 종료일 당일에 이미 `EventPhase.ENDED`로 파생되고, `LocalTime.MAX`는 나노초가 DB 정밀도에서 반올림돼 다음 날 0시로 저장된다). 행사 본문(`mtxt_cn`, NOT NULL)에는 `goal_cn`을 복사한다 — 빈 본문이면 리더가 모집 공고를 백지에서 시작한다.
  - **재제출은 `CHANGES_REQUESTED`에서만 열리고 접수 마감에 막히지 않는다** (#141 · #177). 응답자가 다시 내면 `SUBMITTED`로 돌아가고 `sbmsn_seq`가 1 늘며 `SUBMIT` 이력이 남는다. **반려된 응답의 재제출은 409 `RESPONSE_ALREADY_REJECTED`**로 `RESPONSE_ALREADY_SUBMITTED`와 코드를 나눴다 — "이미 제출했다"는 기다리라는 뜻이지만 반려는 끝났다는 뜻이라, 같은 문구를 돌려주면 응답자는 오지 않을 결과를 기다린다. 이 판정 전부가 `FormResponseHistoryEntity.submit` 하나에 있다(회차를 올리는 자리도 거기뿐이다) — 서비스에 두면 상태 어휘가 늘 때마다 규칙이 호출부마다 복제된다.
    - **마감 예외의 판정 재료도 그 한 곳에서 온다** (#177). "이것이 재제출인가"는 `FormResponseHistoryEntity.isResubmission`(= `CHANGES_REQUESTED`)이 갖고, 회차를 올릴지(`submit`)와 접수 판정을 태울지(`FormResponseServiceImpl.submitResponse`)가 그 하나를 함께 묻는다 — 각자 상태를 비교하면 상태 어휘가 늘 때 한쪽만 고쳐지고, 그 어긋남은 "회차는 올랐는데 마감된 폼이라 거절됐다"처럼 응답자에게만 보이는 모양으로 드러난다. **`FormReceiptPolicy` 자체는 손대지 않았다** — '이 응답만 접수 기간이 연장된 것으로 본다'는 방식(#177 결정 3의 (b))은 그 판정을 부르는 다른 경로(`/public` 조회·초안 저장)까지 뜻이 흔들린다. 그래서 제출 경로의 검사 순서가 바뀌었다: 폼 → **내 응답 조회** → 접수 판정 → 답 검증(재제출인지를 알아야 판정을 태울지 정할 수 있다). 판정 순서 자체(`FORM_NOT_ACCEPTING`이 400보다 먼저다)는 그대로다.
    - **`sbmsn_seq`는 이력 행에도 복사한다.** 응답 행을 따라가 읽으면 그것은 *현재* 회차라, 재제출이 한 번이라도 일어나면 과거 처리까지 새 회차로 보인다.
    - **응답자 쪽 화면 경로는 #177이 열었다** — `GET /v1/forms/{formId}/responses/mine/{formRspnsId}`가 내 답(`rspnsCn`)과 검토 이력을 함께 내려준다(아래 항목). 그전까지는 공개 폼 조회(`alreadySubmitted`)와 자동 저장(`GET`·`PUT .../responses/draft`)이 DRAFT만 다뤄, 응답자가 수정요청 사유를 읽고 이전 답을 불러오는 길이 없었다. **재제출 방식 자체는 그대로 전체 본문 재전송이다** — 재제출 중 임시저장은 열지 않았다(#177 결정 2): 초안 1건 제약(`uk_form_rspns_hstry_one_draft`)과 `rspns_seq` 규칙(#143)을 동시에 건드려야 하고, `CHANGES_REQUESTED` 행의 `rspns_cn`을 직접 갱신하는 길을 열면 "검토자가 보고 있는 내용이 조용히 바뀐다"는 새 문제가 생긴다.
  - 처리 이력 조회는 별도 엔드포인트가 아니라 **응답 상세(`FormResponseDetailResponse.reviewHistories`)에 시간순으로 실린다** — 화면이 상세와 이력을 언제나 함께 그리므로 나누면 상세를 여는 모든 화면이 두 번 요청하고, 두 응답 사이에 다른 검토자의 처리가 끼어들면 화면의 상태와 타임라인이 서로 다른 시점을 가리킨다. 처리가 없으면 빈 배열이다(null이 아니다). 상세의 쿼리는 폼 1 + 응답 1 + 인접 식별자 1 + 이력 1로 네 번이며 이력이 몇 줄이든 그대로다.
  - 상세는 `prevFormRspnsId`·`nextFormRspnsId`를 함께 내린다(`ssccops-web #13`이 이 이름으로 소비한다). 웹이 목록을 들고 계산하지 않는 것은 상세를 URL로 바로 열었을 때(새 탭·북마크) 이동이 죽기 때문이다. 이웃은 **목록의 기본 조회와 같은 순서·범위**에서 고르며, DRAFT 응답을 직접 열면 두 값 모두 null이다 — 이웃을 주면 목록에서 뺀 응답이 이동만으로 심사 흐름에 들어와 DRAFT 제외가 목록에서만 지켜지는 규칙이 된다.
  - 폼 상세의 `responseSummary`(`total`·`submitted`·`changesRequested`·`accepted`·`rejected`)는 #37에서 더했고 #141이 `changesRequested`를 보탰다(`total`에도 들어간다 — 빼면 목록의 `responseCount`와 갈린다). 그전까지 웹은 `res.responseSummary`를 찾지 못해 요약 상자를 늘 0으로 그렸다(터지지 않고 조용히 틀리는 종류). 집계는 `FormResponseCount`를 `(폼, 상태)`로 확장해 **질의 하나**로 받고 목록은 그 합(`total` = `responseCount`)을, 상세는 그대로 쓴다 — 총합용과 상태별을 따로 두면 폼 목록이 폼마다 두 번 집계하고 두 값이 어긋날 여지가 생긴다. 폼 목록은 여전히 폼 1 + 라벨 1 + 집계 1의 3회다.
  - **폼이 CLOSED된 뒤 남은 DRAFT는 접수 종료 후 90일까지 보존하고 그 뒤 삭제한다** (#36 결정). 즉시 지우지 않는 것은 마감 철회(CLOSED→OPEN)가 허용되기 때문이고(#33), 영구 보존하지 않는 것은 미제출 초안이 지원 의사를 남기지 않은 개인정보라 보관 근거가 없기 때문이다. 삭제 배치는 아직 만들지 않았다 — 다중 인스턴스에서 스케줄러 중복 실행을 막을 장치가 없다는, 자동 마감 배치를 두지 않기로 한 것과 같은 이유다. 그때까지는 운영자가 수동으로 정리한다: `DELETE FROM form_rspns_hstry WHERE rspns_stts_cd = 'DRAFT' AND mdfcn_dt < now() - interval '90 days';`
  - **응답 건수는 폼이 정한다** (#143 · `form.mltpl_rspns_yn`). 기본값 false이며 그 폼은 종전대로 한 회원당 1건이다(지원서·설문). true면 한 사람이 여러 건을 낼 수 있다 — 스터디 제안처럼 한 사람이 두 개를 내는 것이 정상인 폼이 실제 대상이다. 판정을 제목·라벨·시스템 폼 여부로 유추하지 않고 컬럼 하나로 둔 것은, 유추 규칙을 만들면 운영자가 제목을 고치는 순간 조용히 뜻이 바뀌기 때문이다(#118의 역할'명' 판정과 같은 종류의 실수). 접수 중에도 켜고 끌 수 있으며, 끄는 것은 "지금부터 새로 낼 수 없다"이지 "이미 낸 것을 무르라"가 아니다 — 그래서 단일 응답 폼인데 한 회원의 응답이 여러 건 남아 있는 상태가 정상적으로 존재할 수 있다. 폼 복제는 이 값을 승계한다(`sys_form_cd`와 갈리는 지점 — 그쪽은 환경에 하나뿐인 이름이라 사본이 가질 수 없다).
  - **UNIQUE는 없애지 않고 옮겼다**: `(form_id, mbr_id)` → **`(form_id, mbr_id, rspns_seq)`**(#143 · `uk_form_rspns_hstry_form_member_seq`). 통째로 떼면 **동시 제출 방어선이 사라진다** — 선조회는 같은 사람이 두 탭에서 동시에 누르는 것을 막지 못하고(둘 다 "없다"를 본다) 그 경합은 자동 저장(#36)에서 이미 겪었다. 단일 응답 폼은 서버가 `rspns_seq`를 1로 고정하므로 두 번째 제출이 여전히 DB에서 걸린다. 위반은 종전처럼 `RESPONSE_ALREADY_SUBMITTED`(제출)·`RESPONSE_SAVE_CONFLICT`(자동 저장)로 옮긴다.
    - **초안은 폼 종류와 무관하게 언제나 1건이다.** 조건부 UNIQUE라 평범한 제약으로 표현되지 않아 PostgreSQL 부분 유니크 인덱스(`uk_form_rspns_hstry_one_draft`, `WHERE rspns_stts_cd = 'DRAFT'`)로 걸고, **H2는 부분 인덱스를 지원하지 않으므로 애플리케이션도 함께 판정한다**(`saveDraft`가 초안이 있으면 새로 만들지 않고 그 행을 갱신한다 — 테스트가 검증하는 것은 이쪽이다). 인덱스만 두면 테스트가 규칙을 확인할 수 없고, 판정만 두면 동시 요청이 그대로 통과한다.
    - **`rspns_seq`(응답 순번)와 `sbmsn_seq`(제출 회차)는 다른 값이다.** 전자는 **새 행이 생길 때**, 후자는 **같은 행을 다시 낼 때** 오른다 — 제안을 두 개 내면 `rspns_seq` 1·2인 두 행이 생기고, 그중 하나가 수정요청을 받아 다시 제출되면 그 행의 `sbmsn_seq`만 2가 된다. 합치면 "2회차 응답"이 두 번째 제안인지 첫 제안의 재제출인지 알 수 없어진다.
    - 순번은 **초안을 만들 때 배정**한다. 표시용 번호가 아니라 UNIQUE를 성립시키는 식별자라 버려진 초안이 번호를 먹어 구멍이 나도 메우지 않는다(메우려면 응답을 지울 때마다 뒤 번호를 당겨야 하고, 그 순간 화면·이력이 가리키던 번호가 다른 응답을 가리킨다). 다음 번호는 행 수가 아니라 **마지막 순번 + 1**이다 — 세면 지워진 적이 있는 순간 이미 쓴 번호를 다시 배정한다.
    - **`prod`·`dev`는 `ddl-auto: update`라 제약 변경을 반영하지 않는다.** 배포 전에 직접 실행할 것: `ALTER TABLE form_rspns_hstry DROP CONSTRAINT uk_form_rspns_hstry_form_member;` · `ALTER TABLE form_rspns_hstry ADD CONSTRAINT uk_form_rspns_hstry_form_member_seq UNIQUE (form_id, mbr_id, rspns_seq);` · `CREATE UNIQUE INDEX uk_form_rspns_hstry_one_draft ON form_rspns_hstry (form_id, mbr_id) WHERE rspns_stts_cd = 'DRAFT';` (컬럼 `rspns_seq`·`mltpl_rspns_yn` 자체는 DEFAULT가 있어 자동으로 붙는다).
  - 제출(`submitResponse`)은 **이어 쓸 응답이 있으면 그 행을 제출로 바꾼다** — 초안이 있으면 그것이고(초안은 최대 1건이라 고를 것이 없다), 없고 수정요청받은 응답이 있으면 순번이 가장 큰 그 행이다. 다중 응답 폼에서 두 뜻이 겹칠 때 재제출을 우선하는 것은 제출 경로에 응답 식별자가 없어 그 행을 지목할 방법이 없기 때문이다(새 응답을 우선하면 수정요청받은 응답을 영영 마무리할 수 없고, 이쪽은 낸 다음에 또 내면 된다). 이어 쓸 것이 없는데 낸 응답이 있으면 단일 응답 폼은 **남아 있는 행의 `submit()`을 불러** 끊는다 — 어느 코드로 끊을지가 상태마다 다르므로(`RESPONSE_ALREADY_SUBMITTED` vs `RESPONSE_ALREADY_REJECTED`, #141) 서비스에 그 표를 옮겨 적지 않는다. 다중 응답 폼은 그 자리를 지나 새 행이 되며, 반려된 응답만 남아 있어도 마찬가지다(#141이 말한 "오조작의 탈출구는 번복이 아니라 새 응답"의 실제 경로). `mbr_id`·`rspns_stts_cd`·`sbmsn_dt`는 전부 서버가 채우며 `sbmsn_dt`는 주입된 `Clock`에서 온다.
  - **자동 저장 API 경로는 그대로다**(`GET`·`PUT /v1/forms/{formId}/responses/draft`, #143에서도 손대지 않았다). 초안이 하나뿐이라 단건 계약이 유지되고 경로에 식별자를 더할 이유가 없다. 단일 응답 폼에서 제출 뒤 초안 저장이 409인 것도 종전 그대로이며, 다중 응답 폼에서는 제출로 초안 자리가 비므로 다음 순번의 새 초안을 만들 수 있다.
  - **`alreadySubmitted`(공개 폼 조회)의 뜻이 좁아졌다** (#143): "냈는가"가 아니라 **"더 낼 수 없는가"**다. 다중 응답 폼에서는 이미 낸 뒤에도 또 내는 것이 정상이라 화면이 작성 폼을 계속 보여줘야 한다 — 필드 이름을 바꾸지 않은 것은 단일 응답 폼(지금 있는 폼 전부)에서 두 뜻이 완전히 같아서다. 대신 `mltplRspnsYn`·`myResponseCount`를 함께 내리고 `submittedAt`은 **마지막** 제출 일시가 된다(다중 응답 폼에서는 `alreadySubmitted`가 false인데 값이 있을 수 있다 — 두 필드가 묻는 것이 다르다).
  - **내 응답 목록은 `GET /v1/forms/{formId}/responses/mine`이다** (#143 · `PublicFormController`). 경로에 `mbrId`를 두지 않는 것은 자동 저장이 세운 규칙 그대로이고, 리터럴 세그먼트가 운영자용 `GET .../responses/{formRspnsId}`보다 먼저 매칭된다(`/draft`와 같은 자리). **접수 가능 여부를 보지 않는다** — 자기가 낸 것을 확인하는 조회라 마감 뒤에도 열려야 하고, 응답을 낼 수 없었던 폼에는 애초에 행이 없어 언제나 빈 배열이다. **작성 중(DRAFT)도 함께 싣는다**(운영자 목록이 DRAFT를 빼는 규칙은 "남의 제출 전 답안이 심사 목록에 섞이지 않게"라 여기에는 해당하지 않는다). 응답 내용(`rspnsCn`)은 싣지 않는다 — 목록이 답하는 것은 "몇 건을 어떤 상태로 냈는가"이고, 답 전체를 읽는 것은 상세(아래)의 몫이다.
  - **제출자용 본인 응답 상세는 `GET /v1/forms/{formId}/responses/mine/{formRspnsId}`다** (#177 · `PublicFormController`). #141이 만든 검토 이력·재제출 흐름에 응답자 쪽 입구를 낸 것이며, 웹의 기획안 작성·재제출 화면이 이 응답 하나로 사유를 보여주고 이전 답을 프리필한다(별도 프리필 API를 두지 않는 근거다). 응답 내용(`rspnsCn`)과 검토 이력(`reviewHistories`)을 함께 싣고, 이력 항목은 **운영자용과 같은 `FormResponseReviewHistoryResponse`를 그대로 재사용한다** — 그래서 **처리자_명(`prcsMbrNm`)이 제출자에게도 보인다**(#177 결정 1: 동아리 내부 결재라 감출 값이 아니고, 학술 승인 이력 #139도 `aprvrMbrNm`을 싣는다. 빼려면 오히려 응답자 전용 이력 DTO를 하나 더 만들어야 한다). 제출(`SUBMIT`) 행도 실려 타임라인이 "제출 → 수정요청 → 재제출 → 승인"으로 읽힌다.
    - **운영자용 `FormResponseDetailResponse`를 재사용하지 않고 얇은 record(`MyFormResponseDetailResponse`)를 새로 만든 것은 `prevFormRspnsId`·`nextFormRspnsId` 때문이다** — 그 둘은 심사 목록의 이웃이라 **정의상 남의 응답 식별자**이고, 내려주면 누가 이 폼에 응답했는지가 이동 버튼으로 드러난다. 회원 정보(`ResponseMemberDetail`)도 뺐다(그 회원은 요청 주체 본인이다). 학술 승인 미리보기(`academicProgramPreview`)도 싣지 않는다 — 검토자가 승인 전에 파싱 결과를 확인하는 값이라(#150) 제출자에게는 누를 버튼이 없고, 미리 실으면 쓰지 않는 계약이 먼저 굳는다.
    - **본인 행이 아니면 없는 응답과 같은 404 `FORM_RESPONSE_NOT_FOUND`다.** 조회에 회원을 함께 거는 것(`findByIdAndFormAndMember`)이 운영자용 상세가 폼을 함께 거는 것과 같은 자리이며 — 그쪽은 폼 경계를, 이쪽은 회원 경계를 지킨다 — 코드를 나누면 그 번호의 응답이 존재하는지가 새어 나간다(응답 식별자는 연속된 정수라 훑는 데 비용이 들지 않는다). 경로에 `mbrId`를 두지 않는 것은 `/draft`·`/mine`이 세운 규칙 그대로이고, `@RequireAuthority`를 붙이지 않는 것은 "본인 또는 관리 권한"이 애노테이션으로 표현되지 않아 서비스에서 끊기 때문이다(#139 승인 이력 조회의 선례). **접수 가능 여부를 보지 않는다** — `/responses/mine` 목록과 같은 기준이며, 오히려 이 조회의 실제 쓰임이 마감 뒤에 있다.
  - 응답 순번(`rspnsSeq`)은 운영자 목록·상세와 제출 응답에도 실린다. 다중 응답 폼에서는 같은 회원의 응답이 여러 행으로 나오므로, 순번이 없으면 운영자는 이름이 같은 두 줄을 제출 일시로만 구별해야 한다.
  - **접수 기간이 끝난 폼은 자동으로 CLOSED가 되지 않는다** — 자동 마감 배치를 두지 않기로 결정했다(#33). 배치가 쓴 CLOSED와 운영자가 누른 CLOSED가 구별되지 않아 '마감 철회'가 무엇을 되돌리는지 알 수 없고, 배치로 닫힌 폼의 종료 일시를 미뤄도 상태가 CLOSED로 남으며, 다중 인스턴스에서 스케줄러 중복 실행을 막을 장치가 없기 때문이다. 대신 목록·상세·전이 응답이 파생 값 `receiptStatus`(`FormReceiptStatus`: DRAFT·SCHEDULED·ACCEPTING·EXPIRED·CLOSED)를 함께 내리고 화면이 그 값으로 배지를 고른다(ssccops-web #9). 저장하지 않고 조회할 때마다 `FormReceiptPolicy`가 다시 계산하므로 `formSttsCd`와 어긋날 수 없다.
  - **템플릿은 폼의 플래그가 아니라 별도 테이블(`form_tmpl`)이다** (#142). 폼에 `tmpl_yn`을 두면 템플릿에는 무의미한 것들(접수 기간·상태·응답)이 전부 "있을 수 없는 값"이 되어 `FormReceiptPolicy`·`FormStatusAction`·응답 집계가 저마다 예외 분기를 하나씩 갖게 되고, 무엇보다 폼을 조회하는 **모든** 자리에 `AND tmpl_yn = false`가 붙어야 한다 — 한 군데만 빠뜨려도 템플릿이 접수 목록에 뜨고 공개 링크가 열린다. 테이블을 나누면 그 필터가 애초에 존재하지 않는다. 컬럼은 `form_tmpl_id`·`tmpl_nm`(V200)·`tmpl_expln`(nullable)·`qitem_cpst_cn`(JSONB)·`use_yn`·`creatr_mbr_id`·`crt_dt`·`mdfcn_dt`이며 **폼과 공유하는 것은 문항 구성 하나**다(`QuestionCompositionContent` 같은 타입).
    - **문항 검증기는 폼과 한 벌이다** — `QuestionCompositionValidator`를 그대로 부르고 템플릿 전용 규칙을 적지 않는다. 두 벌이 되면 템플릿에서는 통과한 구성이 폼 저장에서 거절되고, 사용자가 마주치는 것은 자기가 만들지 않은 폼의 400이다. 방향도 가리지 않는다: 폼→템플릿도 같은 검증기를 다시 태워, 규칙이 늘어난 뒤에 저장돼 있던 옛 폼의 구성이 "저장되지 않는 시작점"으로 굳는 것을 막는다.
    - API는 `GET·POST /v1/form-templates` · `GET·PUT /v1/form-templates/{id}` · `PATCH /v1/form-templates/{id}/use` · `POST /v1/form-templates/{id}/forms`(템플릿 → 폼) · `POST /v1/forms/{formId}/templates`(폼 → 템플릿)다. 목록은 폼 목록과 같은 규칙으로 `qitemCpstCn`을 싣지 않고 문항 수(`qitemCnt`)만 준다. **`DELETE`는 없다** — 내리는 길은 `use_yn`뿐이며 되돌릴 수 있어야 한다(폼 라벨·하위 업무 유형과 같은 축). 사용 여부 전환에 `/use` 세그먼트를 붙인 것은 같은 자원 경로를 `PUT`(수정)이 이미 쓰고 있어서다.
    - 인가는 **컨트롤러 클래스 레벨 `@RequireAuthority(FORM_WRITE)` 하나이고 조회도 예외가 아니다** — `FormResponseController`와 같은 이유로, 핸들러가 하나 늘 때 애노테이션을 빠뜨릴 자리를 만들지 않는다(`FormController`가 핸들러마다 `FORM_READ`/`FORM_WRITE`/`FORM_STATUS_CHANGE`로 나눈 것과 갈리는 지점). 컨트롤러가 `/v1/form-templates`와 `/v1/forms/{formId}/templates` 두 경로를 함께 맡아 클래스 레벨 `@RequestMapping`을 두지 않은 것은 `FormLabelController`와 같다.
    - **템플릿은 공용이다.** 개인 소유 개념을 두지 않으며 `creatr_mbr_id`는 감사용이지 접근 제어에 쓰지 않는다 — 폼 자체에 소유자 개념이 약한데(누구나 남의 폼을 고치고 복제한다) 템플릿에만 도입하면 어휘가 두 벌이 된다.
    - 비활성(`use_yn = false`) 템플릿은 **새 폼의 출발점이 될 수 없을 뿐**이다(400 `FORM_TEMPLATE_NOT_USABLE`). 조회·수정은 그대로 되고(오타를 고친 뒤 다시 켜는 것이 정상 경로다) 그 템플릿으로 이미 만든 폼도 건드리지 않는다 — `FORM_LABEL_NOT_USABLE`과 같은 규칙이며 "새로 고를 수 없다"이지 "달려 있던 것을 떼라"가 아니다. 없는 템플릿은 404 `FORM_TEMPLATE_NOT_FOUND`이고, 폼의 404(공통 `NOT_FOUND`)와 코드를 나눈 것은 한 요청에 폼과 템플릿이 함께 등장하는 경로가 있기 때문이다.
    - **템플릿에서 나온 폼은 그 순간 독립한다.** 상태 DRAFT · 접수 기간 없음 · 라벨 없음이며 문항 구성은 **깊은 복사**다(`FormServiceImpl.duplicate`와 같은 결과이고, 그 규칙의 유일한 구현인 `FormEntity.create(..., DRAFT)`를 두 경로가 함께 부른다). `form`에 템플릿을 가리키는 컬럼을 두지 않은 것은 복사가 한 번뿐인 사건이라서다 — 이후 템플릿을 고쳐도 폼이 따라 바뀌어서는 안 되고, 그래서 "이 템플릿을 쓰는 폼 N건" 같은 집계도 없다. 얕게 넘기면 Hibernate가 두 행에 같은 JSON을 써 "폼을 고쳤더니 템플릿이 바뀐다"가 된다.
    - **폼 복제(`POST /v1/forms/{formId}/duplicate`)는 그대로 남는다.** "이 폼과 똑같은 것 하나 더"는 템플릿과 다른 조작이라 `duplicate`에 "원본이 템플릿인 경우" 분기를 넣지 않았다 — 넣으면 폼 복제 로직이 템플릿을 알아야 하고, 그것이 이 이슈가 피하려던 얽힘이다. 제목 규칙도 갈린다: 복제는 원본 제목 + `(복사본)`이지만 템플릿 → 폼은 요청의 `formTtlNm`이거나 생략 시 템플릿명이다(템플릿명은 폼 제목으로 쓰기에 부적절한 것이 정상이다).
    - 템플릿 수정에는 폼의 문항 식별자 보호(409 `QUESTION_ITEM_IN_USE`)가 없다. 그 규칙이 지키는 것은 `rspns_cn`의 key인데 **템플릿에는 응답이 없다** — 테이블을 나눈 덕에 분기를 두지 않아도 되는 자리다.
- `domain/academicprogram` — 스터디·프로젝트(학술 활동). 활동은 `event`의 1:1 확장이고(`work`가 `oper`를 확장하는 것과 같은 패턴) 모집·팀원 명단은 행사 도메인의 폼·`event_ptcp`를 재사용한다. 행이 생기는 **유일한 경로는 기획안 폼 응답의 승인 이관**(#150)이며 등록 API는 없다 — 폼 도메인은 `SystemFormApprovalHook`으로 그 사실을 모른 채 부르고, 구현체 `ProposalApprovalHook`이 이쪽에 있다.
  - **테이블 7개가 #178에서 데이터사전에 등재되며 이름이 전부 바뀌었다.** `#130`~`#150` 동안 학술만 미등재였고 그 사이 `academic_program`·`curriculum_item`·`session`처럼 풀 영단어를 써, 다른 도메인이 `mbr`·`aprv`·`stts`를 쓰는 자리와 어휘가 갈려 있었다. 지금 이름은 `acdm_actv`(학술_활동) · `acdm_actv_type` · `acdm_actv_aprv` · `crclm_artcl`(교육과정_항목) · `sesn`(회차) · `atndc`(출석) · `file_rfrnc`(파일_참조)다. 새 컬럼·테이블을 더할 때 표준단어를 거치지 않고 풀 영단어를 쓰면 이 도메인만 다시 갈라진다 — 사전 원본은 `private-workspace/spec/데이터사전+테이블컬럼정의서_학술_업데이트_본.xlsx`다.
  - **엔티티의 자바 필드명은 사전을 따라가지 않는다.** `@Column(name = "schdl_cn") private String scheduleText`처럼 컬럼은 표준 약어, 자바 필드는 읽히는 영단어이며 이 레포의 다른 도메인도 같다(`goal_cn` ↔ `goalContent`). **반대로 DTO 필드명은 컬럼에서 유도한다** — `schdlCn`·`pscpMinCnt`·`planYmd`·`actlYmd`·`prgrsCn`·`ntcCn`·`sesnSttsCd`·`aprvSeCd`·`atndYn`·`fileUrlAddr`. 이미 접두를 줄여 쓰던 관례(`typeCd`·`sttsCd`·`academicProgramId`)는 그대로 두었다 — 사전 이름을 그대로 옮기면 `acdmActvAcdmActvTypeCd` 같은 것이 나온다. 회차 목록의 `sort` 파라미터도 이 어휘를 쓰므로 `actlYmd`/`-actlYmd`다.
  - **`acdm_actv_aprv_se_cd`가 `aprv_pnt_cd`가 아닌 이유**는 표준코드 시트의 코드그룹 ID가 유일해야 하기 때문이다 — `aprv_stts_cd` 그룹이 이미 하위 업무용으로 있고 값 집합이 다르다(`NOT_REQUIRED`·`REAPPROVAL_REQUIRED`를 포함한다). 그래서 학술 쪽 두 코드에 `acdm_actv_aprv` 접두를 붙였고, '지점(`pnt`)'은 표준단어 '구분(`se`)'으로 바꿨다(`tkcg_se_cd`·`mtg_se_cd` 선례). 승인자 컬럼도 `aprvr_mbr_id` → `autzr_mbr_id`다.
  - **길이는 표준도메인이 정한다** — `crclm_artcl.ttl` 256(제목V256) · `acdm_actv.schdl_cn` 500(내용V500) · `file_rfrnc.file_url_addr` 255(주소V255) · 코드 컬럼은 전부 20. 이 값을 고칠 때는 **DB보다 먼저 거절하는 파서 상수도 함께 고쳐야 한다**(`ProposalCurriculumParser.MAX_TITLE_LENGTH` · `ProposalResponseParser.MAX_SCHEDULE_TEXT_LENGTH`) — 한쪽만 넓히면 DB가 받는 값을 이관이 거절한다.
  - **`data.sql`은 `@Table`·`@Column`을 따라오지 않는다.** 유형 시드(`acdm_actv_type`의 STUDY·PROJECT) 두 문장이 테이블·컬럼명을 문자열로 박고 있어, 엔티티만 고치면 `local`·`test` 부팅이 그 INSERT에서 곧바로 깨진다. 이름을 바꾸는 변경에서 반드시 함께 본다.
  - **`dev`·`prod`는 `ddl-auto: update`라 리네임이 반영되지 않는다.** #178 시점에는 그래도 손댈 것이 거의 없었다 — `prod`에는 학술 테이블이 아직 없어 첫 릴리스가 처음부터 새 이름으로 만들고, `dev`는 배포 뒤 옛 테이블 7개를 `DROP TABLE ... CASCADE`로 지우면 끝이었다(버려도 되는 QA 데이터였다). **순서를 뒤집으면 안 된다** — 배포보다 먼저 지우면 아직 옛 이름을 쓰는 앱이 그 테이블을 다시 만든다. 앞으로의 리네임은 이 조건이 성립하지 않으므로 `ALTER ... RENAME`이 필요하다.
- `domain/auth` — 로그인·로그아웃 자체는 Supabase(클라이언트) 책임이라 서버에 엔드포인트가 없다. 서버가 답하는 것은 "이 토큰이 우리 서비스의 누구인가" 하나뿐이고 그게 `GET /v1/auth/session`이다. **미가입 사용자에게도 200**으로 응답한다(`signedUp: false`, `member: null`) — 가입이 필요하다는 것도 정상적인 세션 상태이지 오류가 아니며, 403으로 끊으면 프론트가 가입 화면으로 갈 근거를 얻지 못한다. 응답의 `member` 블록(`MemberProfileResponse`)은 회원가입 응답과 같은 모양을 쓴다.
- 관측성: OpenTelemetry(OTLP) + Micrometer(Prometheus/OTLP) + Logstash JSON 로깅(`prod` 프로파일에서만 JSON, 그 외 텍스트 — `logback-spring.xml`)이 이미 연결돼 있다. 로컬에 OTLP collector가 없으면 애플리케이션 종료 시 `Connection refused` 경고가 뜨는데 무해하다.

## 커밋 · 브랜치 · PR 컨벤션

`.github/workflows/`가 강제하는 것과 사람이 지켜야 하는 규칙이 나뉜다 (자세한 배경은 로컬 전용 `.private-workspace/CONTRIBUTING.md` 참고 — git에는 포함되지 않음):

- 브랜치: 이슈 생성 시 `issue-branch-creator.yml`이 제목 앞 태그(`[Feat]`/`[Fix]`/`[Refactor]`)를 읽어 `{type}/#{이슈번호}-{슬러그}` 형식으로 자동 생성. 직접 만들어야 한다면 같은 형식을 따른다.
- 커밋 메시지: 이슈가 있으면 `#{이슈번호} {type}({scope}): 설명`, 없으면 `{type}({scope}): 설명`. 타입은 `pr-labeler.yml`이 그대로 파싱해 PR 라벨을 붙이므로 표기를 벗어나면 라벨이 안 붙는다 — `feat`/`fix`/`refactor`/`design`/`style`/`docs`/`test`/`chore`/`init`/`rename`/`remove`/`cicd`/`hotfix`.
- PR 제목은 `[#이슈번호] 총 작업 내용` — Squash merge 시 그대로 커밋 제목이 되므로 형식을 반드시 지킨다. 이 레포는 Squash and merge만 사용.
- `main`으로 향하는 PR은 `integrate-prod.yml`이 Spotless → Checkstyle → Test/JaCoCo → SonarQube Quality Gate → `bootJar` 순서로 실행되며, Quality Gate를 통과하지 못하면 머지할 수 없다.
- `main`에 머지되면 `deploy-prod.yml`이 GHCR(`ghcr.io/<owner>/ssccops-server`)로 이미지를 푸시하고 Coolify webhook으로 프로덕션 배포를 트리거한다.
