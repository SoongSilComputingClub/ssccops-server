-- ============================================================================
-- V1 — baseline (⚠ 아직 플레이스홀더다)
-- ============================================================================
-- **이 파일은 prod의 현재 스키마 덤프로 통째 교체해야 한다.** 지금 내용은 자리표시일 뿐이며,
-- 교체하기 전에는 Flyway를 켜지 말 것(`spring.flyway.enabled`는 모든 프로필에서 false다).
--
--   pg_dump --schema-only --no-owner --no-privileges -d "<prod 접속 문자열>" > V1__baseline.sql
--
-- 엔티티에서 생성한 DDL을 여기 넣으면 안 된다. prod는 `ddl-auto: update`로 자라난 DB라
-- 엔티티에 없는 고아 컬럼이 남아 있고(`sub_work_type.autzr_role_cd` — ssccops-server#241에서
-- 백필 후 지우지 못했다), 엔티티 기준 DDL로 baseline을 잡으면 첫 `validate`가 바로 터진다.
-- 참고용 DDL(엔티티가 말하는 스키마)이 필요하면 아래로 뽑는다 — **덤프와 대조하는 용도**이며
-- baseline이 아니다. AGENTS.md '스키마 변경' 절에 같은 명령이 있다.
--
-- dev도 같은 명령으로 따로 떠서 prod와 diff 할 것 — 두 환경이 다른 경로로 자랐다면
-- baseline 하나로 둘 다 덮을 수 없다 (ssccops#213 · #241에서 prod만 확인했다).
--
-- 교체한 뒤의 순서는 ssccops-server#262 본문과 AGENTS.md의 '스키마 변경' 절에 있다.
-- ============================================================================

-- 교체 전에 Flyway가 켜지면 여기서 멈추게 한다. 빈 파일로 두면 baseline이 '스키마 없음'으로
-- 기록돼 그 뒤 마이그레이션이 존재하지 않는 테이블을 건드리며 훨씬 늦게, 엉뚱한 곳에서 깨진다.
DO $$
BEGIN
    RAISE EXCEPTION 'V1__baseline.sql이 아직 플레이스홀더다. prod pg_dump 결과로 교체한 뒤 Flyway를 켤 것 (ssccops#213).';
END $$;
